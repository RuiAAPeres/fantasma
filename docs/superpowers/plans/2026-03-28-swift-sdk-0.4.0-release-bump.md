# Swift SDK 0.4.0 Release Bump Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bump Fantasma's Swift-facing release references from `0.3.0` to `0.4.0` and update the sibling StravaPlus repo to consume that new package version.

**Architecture:** Keep the change narrow and release-focused. Update Fantasma's shared version/docs surfaces that advertise the Swift package version, then update StravaPlus's SwiftPM requirement and both checked-in lockfiles so the sibling app tracks the same release target.

**Tech Stack:** Rust workspace metadata, OpenAPI, Mintlify docs, Swift Package Manager, Xcode project metadata

---

### Task 1: Update Fantasma's advertised Swift SDK release version

**Files:**
- Modify: `Cargo.toml`
- Modify: `schemas/openapi/fantasma.yaml`
- Modify: `sdks/ios/README.md`
- Modify: `mintlify/ios-sdk.mdx`
- Modify: `docs/STATUS.md`

- [ ] **Step 1: Record the release bump in project memory**

Add a `Started ...` entry to `docs/STATUS.md` that names the `v0.4.0` Swift SDK bump and points back to this plan.

- [ ] **Step 2: Update the release-facing version references**

Change the shared workspace version and the public iOS install examples from `0.3.0` to `0.4.0`.

- [ ] **Step 3: Verify the Fantasma release surfaces**

Run:

```bash
rg -n '0\.3\.0|0\.4\.0' Cargo.toml schemas/openapi/fantasma.yaml sdks/ios/README.md mintlify/ios-sdk.mdx docs/STATUS.md
swift test --filter FantasmaSDKTests
git diff --check
```

- [ ] **Step 4: Record completion in project memory**

Add a `Completed ...` entry to `docs/STATUS.md` with the fresh verification commands and any release-candidate caveat that matters for downstream consumers.

### Task 2: Update StravaPlus to the new Fantasma package version

**Files:**
- Modify: `/Users/ruiperes/Code/StravaPlus/dash-ios/Dash.xcodeproj/project.pbxproj`
- Modify: `/Users/ruiperes/Code/StravaPlus/dash-ios/Dash.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved`
- Modify: `/Users/ruiperes/Code/StravaPlus/dash-ios/Dash.xcworkspace/xcshareddata/swiftpm/Package.resolved`

- [ ] **Step 1: Bump the package requirement**

Change the Fantasma remote package reference in `project.pbxproj` from exact `0.3.0` to exact `0.4.0`.

- [ ] **Step 2: Refresh both checked-in lockfiles**

Update both StravaPlus `Package.resolved` files so they advertise the same Fantasma package version target.

- [ ] **Step 3: Verify the sibling repo metadata**

Run:

```bash
rg -n '0\.3\.0|0\.4\.0' /Users/ruiperes/Code/StravaPlus/dash-ios/Dash.xcodeproj/project.pbxproj /Users/ruiperes/Code/StravaPlus/dash-ios/Dash.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved /Users/ruiperes/Code/StravaPlus/dash-ios/Dash.xcworkspace/xcshareddata/swiftpm/Package.resolved
plutil -lint /Users/ruiperes/Code/StravaPlus/dash-ios/Dash.xcodeproj/project.pbxproj /Users/ruiperes/Code/StravaPlus/dash-ios/Dash.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved /Users/ruiperes/Code/StravaPlus/dash-ios/Dash.xcworkspace/xcshareddata/swiftpm/Package.resolved
(cd /Users/ruiperes/Code/StravaPlus && git diff --check)
```

- [ ] **Step 4: Surface any release-publishing dependency honestly**

If `v0.4.0` is not yet published to `origin`, call that out instead of pretending the sibling repo already resolved a live remote tag.
