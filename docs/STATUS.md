# Status

## Active
- Completed the March 25, 2026 Hotspot 3 ingest fused-store-path salvage follow-up. `POST /v1/events` still uses the tighter two-phase store path and preserves auth-before-body-read behavior, but `insert_events_with_authenticated_ingest()` now claims the ingest lease before opening the raw-insert transaction instead of folding that claim into the hot insert transaction. That keeps the pre-parse auth plus lifecycle gate from the Hotspot 3 slice while restoring the narrower lease timing that representative append wanted. Fresh verification for the salvaged shape: `cargo fmt --all --check`, `cargo test -p fantasma-core --quiet`, `cargo clippy --workspace --all-targets -- -D warnings`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-ingest --test http --quiet`, targeted Docker-backed store regressions for `authenticate_ingest_request_rejects_invalid_revoked_and_wrong_kind_keys`, `authenticate_ingest_request_rejects_non_active_projects`, and `insert_events_with_authenticated_ingest_inserts_rows`, the new source-shape regression `insert_events_with_authenticated_ingest_keeps_lease_claim_outside_insert_transaction`, and `git diff --check`. Benchmark evidence compares the same clean `origin/main` export in `/tmp/fantasma-main-clean-store-Bu4ox6/artifacts/performance/2026-03-25-hotspot-3-before/summary.md` against the salvaged run in `artifacts/performance/2026-03-25-hotspot-3-after-lease-shape/summary.md`: `live-append-small-blobs` improved from `131584ms` to `130623ms` (`1709.93` to `1722.51` events/s) with checkpoint session / derived p95 effectively flat at `420ms` to `419ms`; `live-append-plus-light-repair` append improved from `134251ms` to `122770ms` (`1675.96` to `1832.68` events/s), repair stayed effectively flat at `7180ms` to `7121ms`, and checkpoint session / derived p95 improved from `636ms` to `626ms`. The first fused-store attempt in `artifacts/performance/2026-03-25-hotspot-3-after/summary.md` remains rejected because it regressed representative append; the salvaged lease shape is the benchmark-backed version to keep.
- Completed the March 25, 2026 stress-repair drain fix. The real bottleneck was not per-install recompute work, but the session repair lane finalizing shared buckets after each `repair_concurrency` slice because the scheduler was still calling a one-slice-named entrypoint directly. The worker now uses `drain_session_repair_queue_with_config()` for that drain-all behavior, the old dead finalize switch is gone from the single-slice helper, and the worker has a focused regression in `crates/fantasma-worker/src/worker.rs` that proves a configured repair queue coalesces four same-bucket repair jobs into one rebuild-finalize pass. Fresh verification in this slice: `cargo fmt --all --check`, `cargo test -p fantasma-core --quiet`, `cargo clippy --workspace --all-targets -- -D warnings`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker worker::tests::repair_queue_with_config_coalesces_same_bucket_rebuilds_across_multiple_jobs --lib -- --exact --nocapture`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker worker::tests::repair_lane_commit_leaves_daily_and_metric_state_final_without_rebuild_queue --lib -- --exact --nocapture`, and `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline --quiet`. Fresh benchmark evidence: clean committed `main` at `2e4f6ba62a5e61f59a6919192e6ffe3173bbd6ba` still fails `stress-repair-30d` with `timed out waiting for repair readiness (session metrics)` and a Stage B trace dominated by `367` rebuild-finalize passes consuming `266804ms`, while the fixed worker passes under `artifacts/performance/2026-03-25-stress-repair-drain-fix-v2/summary.md` with `session_metrics_ready_ms=18372`, `derived_metrics_ready_ms=18372`, and just one shared rebuild-finalize pass in `stage-b.json` (`record_type_counts.rebuild_finalize=1`, `measured_stage_b_ms=17216`, `finalization_metric_rebuild=14681ms`). Representative safety checks under `artifacts/performance/2026-03-25-stress-repair-drain-fix-v2-representative/summary.md` stayed flat overall: `live-append-small-blobs` appended `225000` events in `135580ms` (`1659.53 events/s`) with checkpoint session / derived p95 `423ms`, and `live-append-plus-light-repair` appended `225000` events in `134211ms` (`1676.46 events/s`) with repair uploads in `7170ms` and checkpoint session / derived p95 `633ms`.
- Completed the March 25, 2026 Hotspot 2 staged-combo backout follow-up. The staged pair/triple/quad rebuild rewrite was rejected and removed from `crates/fantasma-worker/src/worker.rs` because its benchmark shape improved repair throughput but moved the append-facing metrics in the wrong direction. The worker is back on the earlier shared-source rebuild SQL, and the follow-up-specific EXPLAIN reuse tests were replaced with lighter SQL-shape regressions that keep the single staged-source statement in place without preserving the rejected combo-ladder cost model. Fresh verification in this follow-up: `cargo test -p fantasma-worker event_metric_rebuild_sql_reuses_one_staged_scope_for_all_cuboids -- --nocapture`, `cargo test -p fantasma-worker session_metric_rebuild_sql_reuses_one_staged_scope_for_all_cuboids -- --nocapture`, `cargo fmt --all --check`, `cargo test -p fantasma-core --quiet`, `cargo clippy --workspace --all-targets -- -D warnings`, and `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline --quiet`. Fresh benchmark evidence is in `artifacts/performance/2026-03-25-hotspot-2-backout-benchmark`, compared against the rejected staged-combo artifact in `artifacts/performance/2026-03-25-hotspot-2-after-staged-combos`: `live-append-small-blobs` recovered from `148398ms` to `134555ms` (`1516.19` to `1672.18` events/s) while checkpoint session / derived p95 moved from `422ms` to `519ms`; `live-append-plus-light-repair` append recovered from `149932ms` to `142809ms` (`1500.68` to `1575.53` events/s), repair regressed from `9059ms` to `14391ms` (`19.87` to `12.51` events/s), and checkpoint session / derived p95 moved from `530ms` to `729ms`; `stress-repair-30d` still failed with `timed out waiting for repair readiness (session metrics)`. Compared to the original Hotspot 2 baseline in `artifacts/performance/2026-03-25-hotspot-2-before`, the backed-out state is effectively flat on the representative append path (`136171ms` to `134555ms`) and still close on mixed append (`141762ms` to `142809ms`) with the same rebuild-heavy timeout. Treat the staged-combo attempt as closed and rejected; Hotspot 2 stays open for a different append-friendly approach.
- Completed the March 25, 2026 Hotspot 2 staged-combo follow-up. `rebuild_event_metric_buckets_in_tx()` and `rebuild_session_metric_buckets_in_tx()` now stage dim2/dim3/dim4 pair, triple, and quad combinations once per helper call instead of rebuilding those join graphs separately for each insert tier, and the worker now has EXPLAIN-backed regressions that assert one base-table scan plus bounded `event_dim_source`, `session_dim_source`, and `install_dim_source` reuse. Fresh verification in this follow-up: `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker worker::tests::event_metric_rebuild_sql_limits_dim_source_reuse_in_explain --lib -- --exact`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker worker::tests::session_metric_rebuild_sql_limits_dim_source_reuse_in_explain --lib -- --exact`, `cargo test -p fantasma-core --quiet`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline --quiet`. Benchmark evidence is in `artifacts/performance/2026-03-25-hotspot-2-after-staged-combos`, compared against the original Hotspot 2 baseline in `artifacts/performance/2026-03-25-hotspot-2-before` and the first shared-source rewrite in `artifacts/performance/2026-03-25-hotspot-2-after`: `live-append-small-blobs` regressed from `136171ms` to `148398ms` (`1652.32` to `1516.19` events/s) while checkpoint session / derived p95 improved from `520ms` to `422ms`; `live-append-plus-light-repair` append regressed from `141762ms` to `149932ms` (`1587.17` to `1500.68` events/s), repair improved sharply from `16258ms` to `9059ms` (`11.07` to `19.87` events/s), and checkpoint session / derived p95 improved from `626ms` to `530ms`; `stress-repair-30d` still failed with `timed out waiting for repair readiness (session metrics)`. Compared to the first shared-source rewrite, the staged-combo follow-up also regressed representative append from `135894ms` to `148398ms` and mixed append from `139095ms` to `149932ms` while improving mixed repair from `13562ms` to `9059ms`. The overall harness verdict stayed `flat`, and the rebuild-heavy gate is still red, so keep the EXPLAIN-backed coverage but do not treat this stronger SQL rewrite as benchmark-approved without a different cost shape or a successful backout/redesign decision.
- Completed the March 25, 2026 Hotspot 2 rebuild CTE benchmark slice. `rebuild_event_metric_buckets_in_tx()` and `rebuild_session_metric_buckets_in_tx()` now issue one staged shared-source CTE statement per helper call instead of rescanning the source relation separately per cuboid tier, and the worker has SQL-shape regressions that lock that structure in place. Fresh verification in this slice: `cargo test -p fantasma-worker event_metric_rebuild_sql_reuses_one_staged_scope_for_all_cuboids -- --nocapture`, `cargo test -p fantasma-worker session_metric_rebuild_sql_reuses_one_staged_scope_for_all_cuboids -- --nocapture`, `cargo test -p fantasma-core --quiet`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline --quiet`, and `git diff --check`. Before/after benchmark evidence is in `artifacts/performance/2026-03-25-hotspot-2-before` and `artifacts/performance/2026-03-25-hotspot-2-after`: `live-append-small-blobs` improved from `136171ms` to `135894ms` (`1652.32` to `1655.69` events/s) and checkpoint session / derived p95 improved from `520ms` to `322ms`; `live-append-plus-light-repair` append improved from `141762ms` to `139095ms` (`1587.17` to `1617.60` events/s), repair improved from `16258ms` to `13562ms`, and checkpoint session / derived p95 moved from `626ms` to `632ms`; `stress-repair-30d` still failed before and after with `timed out waiting for repair readiness (session metrics)`. The overall harness verdict stayed `flat`, so this rewrite is encouraging on representative and light-repair shapes but it did not clear the rebuild-heavy benchmark gate yet. Keep the evidence, but do not treat Hotspot 2 as fully benchmark-approved until the session-repair hard case improves.
- Completed the March 25, 2026 rollup backout benchmark follow-up. Fresh preflight in this follow-up: `cargo test -p fantasma-bench --quiet` and `FANTASMA_ADMIN_TOKEN=test-token docker compose -f infra/docker/compose.bench.yaml config`. Fresh benchmark evidence: `FANTASMA_ADMIN_TOKEN=test-token cargo run -p fantasma-bench -- slo --output-dir artifacts/performance/2026-03-25-rollup-backout-benchmark --scenario live-append-small-blobs --scenario live-append-plus-light-repair`. The current post-backout worker state restored the representative append path and most of the mixed-repair regression versus the rejected interner path, but it did not fully return to the March 24 mixed-append number. Compared to the March 24 post-Hotspot-1 artifact in `artifacts/performance/2026-03-24-rollup-accumulator-after-focus`, `live-append-small-blobs` improved slightly from `134949ms` to `133806ms` (`1667.28` to `1681.54` events/s) with checkpoint session / derived p95 unchanged at `419ms`, while `live-append-plus-light-repair` append regressed from `133360ms` to `136250ms` (`1687.16` to `1651.37` events/s) but repair improved from `16929ms` to `16508ms` and checkpoint session / derived p95 improved from `730ms` to `527ms`. Compared to the rejected interner artifact in `artifacts/performance/2026-03-25-rollup-interner-followup-after`, the backout improved representative append from `142832ms` to `133806ms`, kept mixed append effectively flat at `136280ms` to `136250ms`, improved repair from `17311ms` to `16508ms`, and improved mixed checkpoint session / derived p95 from `629ms` to `527ms`. The harness verdict stayed `flat` overall, so the backout removes the benchmark-regressing interner cost shape, but Hotspot 1 still does not show a clear throughput win over the March 24 accepted checkpoint. Treat this hotspot as retained for the simpler accumulator shape, deterministic ordering coverage, and maintainability, not as a measured throughput win, and stop tuning it further unless profiling identifies a new concrete bottleneck.
- Started the March 25, 2026 rollup backout benchmark follow-up. Scope: rerun the trusted representative plus mixed-repair `fantasma-bench slo` pair against the current post-backout worker state, compare it directly to the March 24 post-Hotspot-1 artifact and the rejected March 25 interner artifact, and record whether removing the interner restores the earlier cost shape.
- Completed the March 25, 2026 rollup interner backout follow-up. Review plus the recorded benchmark regression confirmed that the batch-local string interner changed the cost shape in the wrong direction, so the worker is back on the March 24 hash-accumulator implementation instead of resolving interned strings inside hot-path sort comparators. Kept the useful coverage hardening by extending the event-rollup deterministic-order regression through totals plus dim1, dim2, dim3, and dim4 emission. Fresh verification in this follow-up: `cargo fmt --all --check`, `cargo test -p fantasma-worker event_metric_rollups_keep_tuple_key_order_after_accumulation -- --nocapture`, `cargo test -p fantasma-worker session_metric_store_deltas_keep_tuple_key_order_after_accumulation -- --nocapture`, `cargo test -p fantasma-worker max_dimension_event_metrics_fanout_stays_bounded -- --nocapture`, `cargo test -p fantasma-core --quiet`, `cargo clippy --workspace --all-targets -- -D warnings`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline --quiet`, and `git diff --check`. The March 25 exploratory benchmark artifacts remain in `artifacts/performance/2026-03-25-rollup-interner-followup-after` as evidence for why the interner path was rejected.
- Completed the March 25, 2026 Hetzner reboot hardening follow-up. The March 25 `01:00` host reboot left both `fantasma-prod` and `fantasma-demo` down while the dashboard stack recovered, because the shared Fantasma Compose services had no restart policy. The base Compose file now sets `restart: unless-stopped` on `postgres`, `fantasma-ingest`, `fantasma-api`, and `fantasma-worker`, so both prod and demo inherit the same reboot recovery behavior. Fresh verification in this follow-up: live Hetzner `docker ps -a --format '{{.Names}}\t{{.Status}}' | grep fantasma`, live Hetzner `docker compose --env-file .env.production -f infra/docker/compose.yaml -f infra/docker/compose.prod.yaml -p fantasma-prod ps`, live Hetzner `docker compose --env-file .env.demo -f infra/docker/compose.yaml -f infra/docker/compose.prod.yaml -p fantasma-demo ps`, and `git diff --check`.
- Started the March 25, 2026 rollup interner follow-up. Scope: add the missing event-rollup deterministic-order regression, replace the remaining hot per-insert event-name and dimension-value clones in the Hotspot 1 event/session accumulators with batch-local interned identifiers, rerun the representative plus mixed-repair benchmark pair, and record whether the new cost shape materially improves on the flat March 24 result.
- Completed the March 25, 2026 rollup interner follow-up. The worker now has explicit event-rollup ordering coverage alongside the existing session ordering coverage, and the exploratory event/session accumulator path now interns repeated event names plus built-in dimension values within a batch before resolving them back to public store deltas. Fresh verification in this follow-up: `cargo test -p fantasma-worker batch_string_interner_reuses_ids_for_duplicate_values -- --nocapture`, `cargo test -p fantasma-worker event_metric_rollups_keep_tuple_key_order_after_accumulation -- --nocapture`, `cargo test -p fantasma-worker session_metric_store_deltas_keep_tuple_key_order_after_accumulation -- --nocapture`, `cargo test -p fantasma-worker max_dimension_event_metrics_fanout_stays_bounded -- --nocapture`, `cargo test -p fantasma-core --quiet`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline --quiet`, and `git diff --check`. The benchmark result was worse, not better, versus the March 24 post-Hotspot-1 run in `artifacts/performance/2026-03-24-rollup-accumulator-after-focus`: `live-append-small-blobs` append regressed from `134949` ms to `142832` ms, `live-append-plus-light-repair` append regressed from `133360` ms to `136280` ms, and repair upload time regressed from `16929` ms to `17311` ms. Mixed-repair checkpoint session/derived p95 improved from `730` ms to `629` ms, but the append-path regression means this follow-up is not a net win and should not be treated as merge-ready without a different cost shape. Benchmark artifacts for the exploratory run are in `artifacts/performance/2026-03-25-rollup-interner-followup-after`.
- Started the March 24, 2026 worker rollup accumulator optimization milestone. Scope: capture representative and mixed-repair baseline benchmarks, rewrite the Hotspot 1 event/session rollup accumulators to cut clone/allocation churn while preserving the existing deterministic tuple-key delta ordering, rerun the same benchmark shapes after the change, and record the before/after evidence plus required verification.
- Completed the March 24, 2026 worker rollup accumulator optimization milestone. Event and session rollup accumulation now use hash-backed internal maps keyed by canonical built-in dimension slots, then sort once at emission time to preserve the existing deterministic tuple-key delta order. The iterative representative plus mixed-repair before/after benchmark pair stayed flat overall rather than showing a clear throughput win: `live-append-small-blobs` append improved from `135719` ms to `134949` ms and checkpoint session/derived p95 improved from `420` ms to `419` ms, while `live-append-plus-light-repair` append regressed from `126795` ms to `133360` ms and repair upload time regressed from `15833` ms to `16929` ms, with the harness verdict remaining `flat` for representative append, checkpoint derived, final derived, and mixed repair. Benchmark artifacts live under `/tmp/fantasma-baseline-2026-03-24-hotspot1/artifacts/performance/2026-03-24-rollup-accumulator-baseline-focus` for the clean `HEAD` baseline and `artifacts/performance/2026-03-24-rollup-accumulator-after-focus` for the post-change run. Fresh verification in this slice: `cargo test -p fantasma-worker accumulator_dimension_helpers_preserve_existing_canonical_key_order -- --nocapture`, `cargo test -p fantasma-worker session_metric_store_deltas_keep_tuple_key_order_after_accumulation -- --nocapture`, `cargo test -p fantasma-worker max_dimension_event_metrics_fanout_stays_bounded -- --nocapture`, `cargo test -p fantasma-core --quiet`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline --quiet`, and `git diff --check`.
- Completed the March 24, 2026 metrics contract-hardening follow-up. All five read-key metrics routes now return `409 project_busy` or `409 project_pending_deletion` while a project is `range_deleting` or `pending_deletion`, event discovery now uses the same lifecycle gate as the rest of the metrics surface, OpenAPI now publishes the existing metrics lifecycle and `422` error contracts, the event JSON Schema now matches the live 200-event ingest cap, ingest/query validation now rejects whitespace-padded or over-limit public string values, and event catalog now uses the same public `limit` contract as top events with matching `fantasma-cli` and docs support. Fresh verification in this follow-up: `./scripts/docker-test.sh -p fantasma-api --test auth read_key_metrics_routes_return_409_for_non_active_projects -- --quiet`, `./scripts/verify-changed-surface.sh run api-contract`, and `git diff --check`.
- Started the March 24, 2026 metrics contract-hardening follow-up. Scope: add read-key lifecycle regressions for all five metrics routes, reuse the existing lifecycle gate for discovery, publish the missing metrics `409` and existing `422` contract details in OpenAPI, align the event JSON Schema with the 200-event ingest cap, reject whitespace-padded or over-limit public string fields, and add a bounded public `limit` contract to event catalog plus the matching CLI/docs coverage.
- Completed the March 24, 2026 React Native SDK review follow-up. Fixed the Android bridge so destination handoff now retires superseded clients only after their own in-flight work completes, made `sdks/react-native/fantasma-react-native` self-contained and publishable with corrected `dist/` entrypoints plus bundled native sources/metadata, replaced the placeholder bridge file checks with real package-shape plus native host compilation checks, moved those checks onto temporary npm/module/Gradle state so they do not depend on writable global caches or pollute the package tree, and aligned CI plus the verification matrix with those real bridge checks. Fresh verification in this follow-up: `pnpm --dir sdks/react-native/fantasma-react-native test`, `pnpm --dir sdks/react-native/fantasma-react-native typecheck`, `pnpm --dir sdks/react-native/fantasma-react-native lint`, `pnpm --dir sdks/react-native/fantasma-react-native build`, `pnpm --dir sdks/react-native/fantasma-react-native bridge:ios:check`, `ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools pnpm --dir sdks/react-native/fantasma-react-native bridge:android:check`, `env npm_config_cache=/tmp/fantasma-npm-cache npm pack --json --dry-run --ignore-scripts` from `sdks/react-native/fantasma-react-native` with no `android/local.properties`, `android/.gradle/**`, or `android/build/**` entries, `cd sdks/android && JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools ./gradlew :fantasma-sdk:testDebugUnitTest --tests 'com.fantasma.sdk.reactnative.FantasmaReactNativeBridgeTest'`, `cd sdks/android && JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools ./gradlew :fantasma-sdk:testDebugUnitTest`, `cd sdks/android && JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools ./gradlew :fantasma-sdk:lintDebug :fantasma-sdk:assembleDebugAndroidTest :demo:assembleDebug ktlintCheck detekt`, and `git diff --check`.
- Started the March 24, 2026 React Native SDK review follow-up. Scope: verify the review findings against the new React Native slice, fix the Android destination-handoff timing bug if confirmed, make the package truly publishable/self-contained, and replace the placeholder native bridge checks with real compilation evidence plus matching CI wiring.
- Completed the March 23, 2026 React Native SDK slice. Fantasma now includes a first-party `sdks/react-native/fantasma-react-native` package with an explicit one-live-client JS API, typed JS error mapping, lazy first-use native acquisition with auto-close-on-failure semantics, thin native module wrappers for iOS and Android, bridge managers in the existing Swift and Android SDK layers that preserve destination handoff and real shutdown behavior, repo-level `pnpm` workspace wiring, React Native verification profile updates, CI package checks, and new README/Mintlify docs for the React Native surface. Fresh verification in this slice: `pnpm install`, `pnpm --dir sdks/react-native/fantasma-react-native typecheck`, `pnpm --dir sdks/react-native/fantasma-react-native test`, `pnpm --dir sdks/react-native/fantasma-react-native lint`, `pnpm --dir sdks/react-native/fantasma-react-native build`, `pnpm --dir sdks/react-native/fantasma-react-native bridge:ios:check`, `pnpm --dir sdks/react-native/fantasma-react-native bridge:android:check`, `swift test --package-path . --filter FantasmaReactNativeBridgeTests`, `swift test --package-path . -Xswiftc -strict-concurrency=complete`, `cd sdks/android && JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools ./gradlew :fantasma-sdk:testDebugUnitTest`, and `cd sdks/android && JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools ./gradlew :fantasma-sdk:lintDebug :fantasma-sdk:assembleDebugAndroidTest :demo:assembleDebug ktlintCheck detekt`.
- Started the March 23, 2026 React Native SDK slice. Scope: add a first-party React Native package over the existing iOS and Android Fantasma SDKs, keep the event contract aligned with the current mobile SDKs, preserve existing destination handoff behavior through a thin bridge layer instead of a native storage refactor, wire in repo docs and verification, and support bare React Native plus Expo prebuild/dev-client workflows.
- Completed the March 23, 2026 worker idle-poll follow-up. The repository default for `FANTASMA_WORKER_IDLE_POLL_INTERVAL_MS` is now `2000` ms instead of `250` ms across the worker code, default Compose stacks, and deployment docs, reducing fully idle poll churn while preserving the existing immediate-repoll behavior for busy lanes. Updated the scheduler default regression to match the new value. Fresh verification in this follow-up: `cargo test -p fantasma-worker worker_config_defaults_match_repo_defaults`, `cargo test -p fantasma-worker scheduler::tests::`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets -- -D warnings`.
- Started the March 23, 2026 worker idle-poll follow-up. Scope: raise the default idle-only worker sleep interval after the Hetzner CPU investigation confirmed the deployment would benefit from less aggressive empty-lane polling.
- Completed the March 23, 2026 Hetzner CPU investigation. Host evidence showed `dockerd` and `containerd` burning the bulk of CPU while the prod Fantasma worker itself stayed near 2% CPU, and the immediate cause was idle scheduler log spam: the worker emitted about 950 idle `INFO` lines per minute at the default `250ms` poll interval even with zero backlog. The worker scheduler now logs idle ticks at `DEBUG` instead of `INFO`, keeping busy/error visibility intact while removing the steady Docker log-driver churn. Added a regression test proving idle ticks do not emit `INFO` events. Fresh verification in this slice: remote `top -b -n 1 | head -n 30`, remote `docker stats --no-stream`, remote `docker logs --since=60s fantasma-prod-fantasma-worker-1 | wc -l`, remote `docker exec fantasma-prod-postgres-1 psql -U fantasma -d fantasma -P pager=off -c "select pid, application_name, state, wait_event_type, wait_event, left(query,120) as query from pg_stat_activity where datname = current_database() order by pid;"`, local `cargo test -p fantasma-worker idle_ticks_do_not_emit_info_logs`, local `cargo test -p fantasma-worker scheduler::tests::`, local `cargo fmt --all --check`, and local `cargo clippy --workspace --all-targets -- -D warnings`.
- Started the March 23, 2026 Hetzner CPU investigation. Scope: identify why the live single-host deployment idles at a steady elevated CPU baseline, confirm whether the load comes from Fantasma itself or Docker/runtime overhead, and reduce any avoidable churn without changing worker correctness.
- Completed the March 23, 2026 in-repo Mintlify SDK docs setup. Added a root `docs.json`, a `.mintignore` to keep internal markdown and artifacts out of the docs build, and a `mintlify/` docs surface covering the shipped iOS, Android, and Flutter SDKs. This slice intentionally stops short of repointing public README links because no real hosted Mintlify URL has been assigned yet; once the repo is connected in Mintlify and the live URL is verified, follow up by converting the SDK READMEs into discovery stubs and recording that final public URL here. Fresh verification in this slice: `git diff --check`, `jq empty docs.json`, `docker run --rm -v /Users/ruiperes/Code/fantasma:/work -w /work node:22-bullseye bash -lc 'npx mint --help'`, and `docker run --rm -v /Users/ruiperes/Code/fantasma:/work -w /work node:22-bullseye bash -lc 'npx mint validate'` after adding `.mintignore` to stop Mintlify from parsing repository artifact markdown. The Mintlify CLI still spent the full local run in `validating build...`, so live hosted URL wiring remains unverified.
- Started the March 23, 2026 Mintlify SDK docs launch. Scope: add in-repo Mintlify configuration and public SDK pages for the shipped iOS, Android, and Flutter clients; keep the first pass as a thin migration of the existing SDK docs; and defer root README repointing until a real hosted Mintlify URL exists and is verified.
- Started the March 22, 2026 Flutter SDK slice. Scope: add a first-party Flutter package under `sdks/flutter/fantasma_flutter`, keep the event contract aligned with the existing mobile SDKs, require an explicit `storageNamespace` for persistent client-local queue/identity state, document the Flutter API and behavior, and add Dart/Flutter tests for queueing, locale capture, blocked-destination state, and client isolation.
- Completed the March 22, 2026 Flutter SDK slice. Fantasma now includes a first-party Flutter package under `sdks/flutter/fantasma_flutter` with an instance-based `FantasmaClient` API, explicit `storageNamespace` persistence boundaries, SQLite-backed durable queueing, typed config/upload errors, locale snapshotting at enqueue time, blocked-destination persistence, and client-local lifecycle-triggered best-effort flush scheduling. Added Flutter/Dart regression tests for config normalization, locale capture, queue persistence, blocked-destination restart behavior, same-destination client isolation, and flush coalescing, and updated the root README plus architecture/SDK docs to describe the new Flutter surface. Fresh verification in this slice: `git diff --check`, `docker run --rm -v /Users/ruiperes/Code/fantasma:/work -w /work/sdks/flutter/fantasma_flutter ghcr.io/cirruslabs/flutter:3.29.3 bash -lc 'flutter pub get && flutter test'`, and `docker run --rm -v /Users/ruiperes/Code/fantasma:/work -w /work/sdks/flutter/fantasma_flutter ghcr.io/cirruslabs/flutter:3.29.3 bash -lc 'flutter pub get && flutter analyze'`.
- Completed the March 22, 2026 Flutter SDK review follow-up. Threshold-triggered uploads no longer make `track()` perform inline network I/O or surface best-effort upload failures, malformed `202 Accepted` envelopes now require an exact `accepted == batch_size` match before rows are deleted, and the runtime now rejects a second live client for the same `storageNamespace` instead of implying concurrent shared-namespace safety. Added ignore rules for Flutter build artifacts so generated `build/`, `.dart_tool/`, and `pubspec.lock` outputs stay out of source control. Fresh verification in this follow-up: `git diff --check`, `docker run --rm -v /Users/ruiperes/Code/fantasma:/work -w /work/sdks/flutter/fantasma_flutter ghcr.io/cirruslabs/flutter:3.29.3 bash -lc 'flutter pub get && flutter test'`, and `docker run --rm -v /Users/ruiperes/Code/fantasma:/work -w /work/sdks/flutter/fantasma_flutter ghcr.io/cirruslabs/flutter:3.29.3 bash -lc 'flutter pub get && flutter analyze'`.
- Started the March 22, 2026 Swift SDK `v0.3.0` release cut. Scope: bump the root package-facing version references from `0.2.0` to `0.3.0`, record the release in project memory, verify the Swift package still passes, and publish an annotated `v0.3.0` tag from the current `main` commit so downstream apps can adopt the locale-emitting SDK without pinning an arbitrary git revision.
- Completed the March 22, 2026 Swift SDK `v0.3.0` release cut. The root workspace version, OpenAPI info version, and iOS package-install example now all point at `0.3.0`, and the tagged release is intended to carry the locale-emitting SDK changes that landed after `v0.2.0` so downstream apps can adopt them through a normal package version instead of a raw revision pin. Fresh verification in this release cut: `rg -n "0\\.2\\.0|0\\.3\\.0" Cargo.toml schemas/openapi/fantasma.yaml sdks/ios/README.md docs/STATUS.md`, `swift test --filter FantasmaSDKTests`, `git tag --sort=-creatordate | head -n 5`, `git tag -a v0.3.0 -m "FantasmaSDK v0.3.0"`, `git push origin main`, and `git push origin v0.3.0`.
- Completed the March 22, 2026 verification-process hardening slice. `AGENTS.md` now explicitly requires full Docker-backed suite coverage for backend/API/worker contract changes, forbids repushes after red CI until the exact failing job is reproduced locally, and points agents at the checked-in verification matrix helper. Added [`docs/verification.md`](./verification.md) as the canonical local verification matrix and added [`scripts/verify-changed-surface.sh`](../scripts/verify-changed-surface.sh) plus [`scripts/tests/verify-changed-surface-audit.sh`](../scripts/tests/verify-changed-surface-audit.sh) so common change surfaces map to explicit commands instead of memory. Fresh verification in this slice: `bash scripts/tests/verify-changed-surface-audit.sh`, `bash -n scripts/verify-changed-surface.sh scripts/tests/verify-changed-surface-audit.sh`, `bash scripts/verify-changed-surface.sh list`, `bash scripts/verify-changed-surface.sh print api-contract`, and `bash scripts/verify-changed-surface.sh print worker-contract`.
- Completed the March 22, 2026 built-in `group_by` restore. Public `GET /v1/metrics/events` and bucketed `GET /v1/metrics/sessions` now accept built-in `group_by` over `platform`, `app_version`, `os_version`, and `locale` through D4, while exact-range `active_installs` now accepts built-in filters plus `group_by` through D2 and rejects wider requests before execution. The API now uses generalized subset synthesis for D0..D4 grouped reads, `active_installs` no longer exposes the public distinct-count fallback, the store aggregate readers now span D3/D4 event and session cuboids, the CLI again serializes repeated `--group-by`, and the OpenAPI/docs/specs now reflect the restored contract. Fresh verification in this slice: `cargo test -p fantasma-api --lib --quiet`, `cargo test -p fantasma-cli --test http_flows --quiet`, `cargo test -p fantasma-worker --test pipeline --no-run`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_exposes_grouped_event_metrics_up_to_four_built_in_dimensions -- --exact --nocapture`, and `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_exposes_dim2_session_metrics_and_active_install_overlaps -- --exact --nocapture`.
- Completed the March 22, 2026 closed-event-contract review follow-up. `active_installs` reads once again use the precomputed bitmap path for zero, one, and two built-in filters and only fall back to `COUNT(DISTINCT install_id)` for three-plus built-ins; legacy ingest `properties` payloads now return the indexed validation envelope instead of a generic parse error; and the Android SDK now derives `locale` from the app context configuration so per-app language overrides win over the process default. Added regressions for the active-install read-strategy split, legacy `properties` normalization, and Android app-locale serialization. Fresh verification in this follow-up: `cargo test -p fantasma-core --quiet`, `cargo test -p fantasma-api --lib --quiet`, `cd sdks/android && JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ./gradlew :fantasma-sdk:testDebugUnitTest --tests 'com.fantasma.sdk.internal.FantasmaRuntimeBehaviorTest'`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `./scripts/docker-test.sh -p fantasma-ingest --test http --quiet`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline --quiet`, and `git diff --check`.
- Completed the March 21, 2026 closed-event-contract slice. Fantasma now rejects arbitrary event `properties` repo-wide, carries optional top-level `locale` as a first-class built-in field, removes public `group_by` from metrics reads and shared types, keeps `live_installs` intentionally unfiltered, constrains public filter vocabularies to `platform`, `app_version`, `os_version`, and `locale`, and widens the bounded event/session cube path to support any combination of those four built-ins. The slice also removed property-shaped CLI/SDK APIs, updated the public schemas/docs, and fixed the migration numbering collision by renaming the new four-built-in cube migration to `0021`. Fresh verification in this slice: `cargo test -p fantasma-core --quiet`, `cargo test -p fantasma-api --lib --quiet`, `cargo test -p fantasma-cli --test http_flows --quiet`, `cargo test -p fantasma-bench --quiet`, `swift test --package-path /Users/ruiperes/Code/fantasma --filter FantasmaSDKTests`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `./scripts/docker-test.sh -p fantasma-ingest --test http --quiet`, `./scripts/docker-test.sh -p fantasma-api --test auth --quiet`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline --quiet`, and `./scripts/cli-smoke.sh`.
- Started the March 20, 2026 Rust CI break investigation. Scope: reproduce the `Rust Checks` -> `cargo test --workspace --quiet` failure from CI run #74 on commit `b8c949a`, identify the exact flaky or deterministic regression behind the exit-101 test step, and land the smallest regression-tested fix that restores the workflow without widening production scope.
- Completed the March 20, 2026 Rust CI break investigation. CI run #74 on commit `b8c949a` failed in `worker::tests::rebuild_finalization_sweeps_visible_project_queue_in_one_pass`, but the underlying rebuild sweep logic was still draining the queue correctly. The regression was in the test: it counted every `rebuild_finalize` trace line in a process-global trace file, so unrelated parallel worker activity could append a second finalization record and trip the `len() == 1` assertion. The test now deterministically seeds unrelated trace noise, then filters for the specific sweep record produced by the `513`-bucket drain before asserting on finalization timing. Fresh verification in this repair: `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker worker::tests::rebuild_finalization_sweeps_visible_project_queue_in_one_pass --lib -- --exact --nocapture`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh --quiet`.
- Started the March 19, 2026 Android Kotlin SDK slice. Scope: add an Android-first Kotlin SDK with Swift-parity wire contract and runtime behavior, but an intentionally instance-based Android API; stand up the Android Gradle build, durable queue/runtime implementation, demo app, and the doc updates needed to replace the old “same public API as iOS” placeholder language.
- Completed the March 19, 2026 Android Kotlin SDK slice. Fantasma now has a real Android Gradle workspace under `sdks/android`, a first `:fantasma-sdk` library with an instance-based Kotlin `FantasmaClient` API, destination-scoped durable Room queue/runtime behavior aligned with the Swift SDK contract, Android instrumentation coverage for queue persistence and install-identity rotation, and a runnable `:demo` app under `apps/demo-android`. Docs now explicitly say cross-platform parity is at the event contract and runtime-behavior level rather than identical public APIs, and the stale iOS batching text now matches the shipped `30 seconds / 100 events` defaults. Fresh Android verification in this slice: `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools ./gradlew :fantasma-sdk:testDebugUnitTest`, `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools ./gradlew :fantasma-sdk:testDebugUnitTest :fantasma-sdk:lintDebug :fantasma-sdk:assembleDebugAndroidTest :demo:assembleDebug ktlintCheck detekt`, and `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools ./gradlew :fantasma-sdk:connectedDebugAndroidTest` (expected infrastructure failure: `No connected devices!`).
- Started the March 19, 2026 Android SDK review-follow-up. Scope: address the first post-implementation review findings by making superseded Android client handles fail deterministically instead of silently losing events, restoring Swift-parity explicit-flush wait semantics, and fixing the demo app's missing network permission while aligning the Android docs with the one-active-destination runtime model.
- Completed the March 19, 2026 Android SDK review-follow-up. Different Android destinations are now documented and enforced as one-active-destination-per-process semantics, superseded client handles fail with `FantasmaException.ClosedClient` instead of silently accepting work, explicit `flush()` now waits for an in-flight upload to finish, and the demo app manifest now requests `android.permission.INTERNET`. Added regression coverage for superseded-handle behavior, in-flight flush waiting, and the demo manifest permission. Fresh verification in this follow-up: `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools ./gradlew :fantasma-sdk:testDebugUnitTest --tests 'com.fantasma.sdk.internal.FantasmaRuntimeBehaviorTest' :demo:testDebugUnitTest --tests 'com.fantasma.demo.AndroidManifestTest'`, `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools ./gradlew :fantasma-sdk:testDebugUnitTest :demo:testDebugUnitTest :fantasma-sdk:lintDebug :fantasma-sdk:assembleDebugAndroidTest :demo:assembleDebug ktlintCheck detekt`, and `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools ./gradlew :fantasma-sdk:connectedDebugAndroidTest` (expected infrastructure failure: `No connected devices!` when no emulator/device is attached).
- Started the March 19, 2026 Android SDK lifecycle-retirement follow-up. Scope: fix the remaining review gap where superseded runtimes stayed registered and alive until an explicit `close()` on stale client handles, ensuring destination replacement retires orphaned background/periodic machinery and stale closes cannot release a newer runtime that reused the same destination signature.
- Completed the March 19, 2026 Android SDK lifecycle-retirement follow-up. Superseding a destination now evicts the old runtime from the registry immediately, the superseded runtime retires itself after the current upload boundary without requiring the stale handle to call `close()`, and stale `close()` calls no longer release or shut down a newer runtime that reused the same destination signature. Added regression coverage for runtime-registry stale-handle release and for switching back to a previously used destination after supersession. Fresh verification in this follow-up: `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools ./gradlew :fantasma-sdk:testDebugUnitTest --tests 'com.fantasma.sdk.internal.RuntimeRegistryTest' --tests 'com.fantasma.sdk.internal.FantasmaRuntimeBehaviorTest'`, `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools ./gradlew :fantasma-sdk:testDebugUnitTest :demo:testDebugUnitTest :fantasma-sdk:lintDebug :fantasma-sdk:assembleDebugAndroidTest :demo:assembleDebug ktlintCheck detekt`, and `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools ./gradlew :fantasma-sdk:connectedDebugAndroidTest` (expected infrastructure failure: `No connected devices!` when no emulator/device is attached).
- Started the March 20, 2026 Android SDK stale-close boundary follow-up. Scope: close the last supersede-lifecycle gap where a stale `close()` on an already-evicted runtime still shut it down before its current upload boundary finished, and add an explicit regression that exercises supersede + in-flight flush + stale close in the same path.
- Completed the March 20, 2026 Android SDK stale-close boundary follow-up. `RuntimeRegistry.release()` now treats missing or mismatched runtime entries as stale-handle no-ops instead of reporting a normal last-handle release, so an evicted superseded runtime can finish its current upload boundary and retire itself. Added a regression covering supersede + in-flight flush + stale close in one path. Fresh verification in this follow-up: `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools ./gradlew :fantasma-sdk:testDebugUnitTest --tests 'com.fantasma.sdk.internal.RuntimeRegistryTest' --tests 'com.fantasma.sdk.internal.FantasmaRuntimeBehaviorTest'`, `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools ./gradlew :fantasma-sdk:testDebugUnitTest :demo:testDebugUnitTest :fantasma-sdk:lintDebug :fantasma-sdk:assembleDebugAndroidTest :demo:assembleDebug ktlintCheck detekt`, and `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools ./gradlew :fantasma-sdk:connectedDebugAndroidTest` (expected infrastructure failure: `No connected devices!` when no emulator/device is attached).
- Started the March 19, 2026 grouped-session readiness CI stabilization follow-up. Scope: investigate the new `main` CI failure after the core/SDK regression coverage push, verify whether the CLI smoke path or the Rust workspace test matrix actually regressed, and reduce the failing worker regression to the smallest CI-stable shape before changing any production code.
- Completed the March 19, 2026 grouped-session readiness CI stabilization follow-up. The CLI dogfood smoke path stayed green; the real failure was `pipeline_readiness_poll_for_grouped_session_counts_stays_200_and_converges` timing out inside `cargo test --workspace --quiet` under GitHub runner contention. The fix keeps production code unchanged and shrinks only that readiness-convergence regression because the adjacent catch-up test already proves grouped session queries stay `200` while bounded worker batches advance. Fresh verification in this follow-up: `DATABASE_URL=postgres://fantasma:fantasma@127.0.0.1:55432/fantasma cargo test -p fantasma-worker --test pipeline pipeline_readiness_poll_for_grouped_session_counts_stays_200_and_converges -- --nocapture` (plus 3 repeated runs), `DATABASE_URL=postgres://fantasma:fantasma@127.0.0.1:55432/fantasma cargo test -p fantasma-worker --test pipeline pipeline_keeps_grouped_session_metrics_day_queries_200_during_worker_catchup -- --nocapture`, `DATABASE_URL=postgres://fantasma:fantasma@127.0.0.1:55432/fantasma cargo test --workspace --quiet`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets -- -D warnings`.
- Started the March 19, 2026 Rust core + iOS SDK audit gap-coverage slice. Scope: convert the audit findings into focused regression tests for Rust event/auth boundary behavior plus SDK destination-normalization and upload-failure edges, keeping production behavior unchanged unless a failing test proves a contract bug.
- Completed the March 19, 2026 Rust core + iOS SDK audit gap-coverage slice. Added `fantasma-core` regressions for event payload byte-limit and property-key boundary validation, `fantasma-auth` regressions for missing/invalid admin credentials plus short-key prefix fallback, and SDK regressions for destination normalization equivalence/path drift plus terminal-vs-retryable ingest failures. The iOS SDK uploader now treats `401 unauthorized` and `422` ingest rejects as blocked-destination failures so the same misconfigured destination stops retrying until changed, while `500` and unrelated `409` responses remain retryable. Fresh verification in this slice: `cargo test -p fantasma-core events::tests -- --nocapture`, `cargo test -p fantasma-auth -- --nocapture`, `swift test --package-path /Users/ruiperes/Code/fantasma --filter FantasmaSDKBehaviorTests`, `cargo test -p fantasma-core -p fantasma-auth`, `cargo test -p fantasma-api --lib`, `swift test --package-path /Users/ruiperes/Code/fantasma`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets -- -D warnings`.
- Started the March 19, 2026 project-and-range deletion API slice. Scope: add operator-only full-project purge plus explicit range-delete jobs under `/v1/projects/*`, extend projects with lifecycle state and fencing epochs, add independent deletion-job history plus processing leases, gate ingest/read/management mutations by project state, expose the workflow through `fantasma-cli`, and keep the iOS SDK on the existing public `uploadFailed` surface while adding an internal blocked-destination latch for `409 project_pending_deletion`.
- Completed the March 19, 2026 project-and-range deletion API slice. Fantasma now exposes operator-only full-project purge plus explicit range-delete jobs under `/v1/projects/*`, persists lifecycle state and fence epochs on projects, records independent historical deletion jobs plus project-processing leases, executes fenced range-delete rebuilds and explicit full-project purge work in `fantasma-worker`, surfaces the workflow through `fantasma-cli`, and keeps the iOS SDK on the existing public `uploadFailed` path while internally distinguishing transient `409 project_busy` from latched `409 project_pending_deletion`. Verification in this slice included: `cargo fmt --all`, `cargo fmt --all --check`, `cargo test -p fantasma-store --lib --no-run`, `cargo test -p fantasma-api --lib --no-run`, `cargo test -p fantasma-api --test auth --no-run`, `cargo test -p fantasma-ingest --lib --no-run`, `cargo test -p fantasma-worker --lib --no-run`, `cargo test -p fantasma-worker --test pipeline --no-run`, `cargo test -p fantasma-cli --test http_flows --no-run`, `swift test --filter projectBusyDoesNotBlockDestination`, `swift test --filter projectPendingDeletionBlocksDestinationUntilChanged`, `swift test --filter changedDestinationClearsBlockedLatch`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline range_delete_worker_rebuilds_state_and_reactivates_project -- --exact --quiet`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline project_purge_worker_removes_project_and_keeps_history -- --exact --quiet`, `cargo clippy --workspace --all-targets -- -D warnings`, `bash scripts/tests/cli-smoke-portability.sh`, and `./scripts/cli-smoke.sh`.
- Completed the March 19, 2026 fence-sequencing regression follow-up for project deletion. Added store regressions that `insert_events`, `rename_project`, `create_api_key`, and `revoke_api_key` fail closed once a project is no longer `active`, plus a worker pipeline regression proving deletion jobs remain blocked while older processing leases exist and only run after those leases clear. Verification in this follow-up included: `cargo fmt --all`, `cargo fmt --all --check`, `cargo test -p fantasma-store --lib --no-run`, `cargo test -p fantasma-worker --test pipeline --no-run`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-store project_processing_lease_detects_fence_epoch_advance -- --quiet`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-store insert_events_rejects_non_active_projects -- --quiet`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-store rename_and_key_mutations_reject_non_active_projects -- --quiet`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker project_deletion_waits_for_older_processing_leases -- --quiet`, and `cargo clippy --workspace --all-targets -- -D warnings`.
- Hardened the March 19, 2026 deletion/fencing test matrix around scheduler predicates, historical management routes, and operator CLI flows. Added store regressions proving stale older-epoch leases are excluded from deletion blockers and queued deletion selection skips lease-blocked projects, API regressions proving historical successful purge `DELETE` returns the latest successful job after project-row removal, historical deletion history does not keep `/v1/projects/{id}/keys` alive once the row is gone, and mismatched `{project_id}/{deletion_id}` lookups fail closed, plus CLI HTTP-flow regressions for `fantasma projects delete`, `fantasma projects deletions list`, `fantasma projects deletions get`, and `fantasma projects deletions range` using the active project and serializing the expected range-delete scope. Fresh verification in this follow-up: `cargo test -p fantasma-cli --test http_flows projects_delete_uses_active_project -- --exact`, `cargo test -p fantasma-cli --test http_flows project_deletions_list_uses_active_project -- --exact`, `cargo test -p fantasma-cli --test http_flows project_deletions_get_uses_active_project -- --exact`, `cargo test -p fantasma-cli --test http_flows project_deletions_range_sends_expected_scope -- --exact`, `cargo test -p fantasma-store --lib --no-run`, `cargo test -p fantasma-api --test auth --no-run`, `cargo test -p fantasma-cli --test http_flows --no-run`, `cargo test -p fantasma-worker --test pipeline --no-run`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-store count_project_processing_leases_ignores_stale_leases -- --quiet`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-store claim_next_project_deletion_job_skips_blocked_projects -- --quiet`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-api --test auth delete_project_returns_latest_successful_historical_purge -- --exact --quiet`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-api --test auth historical_keys_return_not_found_after_project_row_removal -- --exact --quiet`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-api --test auth deletion_get_returns_not_found_for_mismatched_project -- --exact --quiet`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline blocked_deletion_does_not_head_of_line_block_other_projects -- --quiet`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline stale_processing_leases_do_not_block_deletion_forever -- --quiet`, `cargo fmt --all`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets -- -D warnings`.
- Fixed the March 19, 2026 Rust CI regression introduced by the deletion/fencing hardening pass. The grouped-session worker was timing out its pool in `cargo test --workspace --quiet` because each incremental session-apply task held one connection open for its transaction and then tried to refresh the external project lease through the pool before commit, effectively demanding a second connection per concurrent task. I added a focused worker regression proving concurrent incremental session apply does not require an extra pool checkout for lease refresh, then removed the in-transaction heartbeat refresh from that short-lived per-install path so the already-visible external lease is verified and released without exhausting the pool. Fresh verification in this fix: `DATABASE_URL=postgres://fantasma:fantasma@127.0.0.1:55000/fantasma cargo test -p fantasma-worker --lib worker::tests::incremental_session_apply_does_not_need_extra_pool_connection_for_lease_refresh -- --exact`, `DATABASE_URL=postgres://fantasma:fantasma@127.0.0.1:55000/fantasma cargo test -p fantasma-worker --test pipeline pipeline_returns_grouped_session_platform_app_version_counts -- --exact`, `DATABASE_URL=postgres://fantasma:fantasma@127.0.0.1:55000/fantasma cargo test -p fantasma-worker --test pipeline pipeline_returns_grouped_session_platform_app_version_counts_for_benchmark_like_history -- --exact`, `DATABASE_URL=postgres://fantasma:fantasma@127.0.0.1:55000/fantasma cargo test --workspace --quiet`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets -- -D warnings`.
- Started the March 19, 2026 operator usage-events API slice. Scope: add an operator-authenticated `/v1/usage/events` endpoint backed by accepted raw-event counts on `received_at`, share the typed usage contract through `fantasma-core`, expose the read through `fantasma-cli`, and extend the CLI smoke/manual operator docs so raw usage is proven separately from worker-derived metrics.
- Completed the March 19, 2026 operator usage-events API slice. Fantasma now exposes operator-authenticated raw accepted-event usage at `GET /v1/usage/events`, shares the typed `ProjectSummary` / `UsageProjectEvents` / `UsageEventsResponse` contract through `fantasma-core`, surfaces the workflow through `fantasma-cli usage events`, and proves the operator flow in `scripts/cli-smoke.sh` immediately after ingest so raw `received_at` usage stays distinct from the later worker-derived metrics poll. Verification in this slice included: `cargo test -p fantasma-core metrics::tests::usage_events_response_serializes_shape -- --exact`, `cargo test -p fantasma-api --lib usage_events`, `cargo test -p fantasma-api http::tests::openapi_documents_scoped_auth_and_event_metrics_contract -- --exact`, `cargo test -p fantasma-cli --test http_flows`, `./scripts/docker-test.sh -p fantasma-store tests::list_usage_events_counts_received_at_and_omits_empty_projects -- --exact`, `./scripts/docker-test.sh -p fantasma-api --test auth usage_events_route_requires_operator_auth_and_counts_received_at -- --exact`, `bash -n scripts/cli-smoke.sh`, `bash scripts/tests/cli-smoke-portability.sh`, and `./scripts/cli-smoke.sh`.
- Completed the March 19, 2026 usage-events review hardening follow-up. `fantasma usage events --json` now returns the raw API body instead of re-serializing the shared DTO, so additive response fields survive CLI output, and `scripts/cli-smoke.sh` now captures a start/end UTC day window around ingest before asserting raw usage so the accepted-event proof does not flake across midnight. Fresh verification in this follow-up: `cargo test -p fantasma-cli --test http_flows usage_events -- --nocapture`, `bash -n scripts/cli-smoke.sh`, and `bash scripts/tests/cli-smoke-portability.sh`.
- Started the March 19, 2026 demo-stack benchmark workflow follow-up. Goal: replace the ad hoc SSH Python against the deployed demo stack with a checked-in, repeatable operator workflow in the Fantasma repo.
- Completed the March 19, 2026 demo-stack benchmark workflow follow-up. Fantasma now includes [demo-stack-burst-bench.py](/Users/ruiperes/Code/fantasma/scripts/demo-stack-burst-bench.py) plus [run-demo-stack-burst-bench.sh](/Users/ruiperes/Code/fantasma/scripts/run-demo-stack-burst-bench.sh), which together benchmark the three realistic `300`-event burst scenarios directly against an existing remote `fantasma-demo` stack by sourcing the remote `.env.demo` and deriving the loopback demo ports. Fresh verification in this slice: `python3 -m py_compile scripts/demo-stack-burst-bench.py`, `bash -n scripts/run-demo-stack-burst-bench.sh`, and `./scripts/run-demo-stack-burst-bench.sh --host user@example-host`. That live run returned: `300 x 1 -> session_ready_from_burst_start_s 1.440`, `150 x 2 -> 0.975`, `100 x 3 -> 0.839`.
- Started the March 19, 2026 realistic-burst benchmark follow-up. Goal: codify the host-measured `300`-event customer-shaped readiness cases inside `fantasma-bench` without disturbing the existing representative, mixed-repair, stress, or read-visibility suites.
- Completed the March 19, 2026 realistic-burst benchmark follow-up. `fantasma-bench slo` now includes a dedicated `burst-readiness` suite with `burst-readiness-300-installs-x1`, `burst-readiness-150-installs-x2`, and `burst-readiness-100-installs-x3`, all chunked as exact `3 x 100` ingest requests so the benchmark matches the measured customer-shaped burst cases while keeping the existing large stress windows intact. The SLO hard-gate expectation model now tracks exact `plan=pro` filtered totals from scenario data instead of assuming the old full representative baseline, so grouped readiness checks remain exact for both the new burst scenarios and the existing stress/live suites. `docs/performance.md` now explains when to run `burst-readiness` versus `stress`. Fresh verification in this slice: `cargo test -p fantasma-bench --quiet`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets -- -D warnings`.
- Started the March 19, 2026 ingest-batch and iOS flush-defaults follow-up. Scope is intentionally narrow: raise the public ingest batch cap from `100` to `200`, raise the iOS SDK default upload batch size from `50` to `100`, and relax the SDK periodic flush timer from `10s` to `30s` without changing the worker or metrics-readiness architecture.
- Completed the March 19, 2026 ingest-batch and iOS flush-defaults follow-up. The public ingest contract now accepts up to `200` events per batch, the iOS SDK live defaults now send up to `100` queued events per upload and wait `30s` between periodic flush attempts, and the OpenAPI contract plus SDK/internal ingest tests now lock the new envelope in place. Fresh verification in this slice: `./scripts/docker-test.sh -p fantasma-ingest --test http`, `swift test --filter FantasmaSDKInternalTests`, `swift test`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets -- -D warnings`.
- Completed the March 19, 2026 docs-and-remote-benchmark follow-up for the new ingest/SDK defaults. `docs/architecture.md`, `docs/deployment.md`, and `sdks/ios/FantasmaSDK/README.md` now describe the `200`-event ingest cap plus the iOS live defaults of `100` queued events per upload and a `30s` periodic flush cadence. Fresh deployed demo-stack benchmark evidence on commit `619ee81`: write-only ingest improved from `37.4k` to `49.8k` events/s at concurrency `8` and from `42.4k` to `47.6k` events/s at concurrency `16` when moving synthetic batch size from `100` to `200`; bounded readiness on `20k` events at concurrency `8` stayed mixed, with the `100`-event run showing `event_metrics_ready_s 0.515` and `session_metrics_ready_s 104.247`, while the later `200`-event run under continued benchmark load timed out at `120s` with `event_metrics_last_value 19800` and `session_metrics_last_value 1936`.
- Completed the March 19, 2026 deploy-default follow-up for the new git-backed host flow. `scripts/deploy-host.sh` now deploys `fantasma-prod` by default, adds `--with-demo` for the cases where both stacks should move together, and adds `--demo-only` for the rare demo-only refresh. `docs/deployment.md` now documents the prod-only default so routine early-stage deploys do not churn the mocked demo stack unnecessarily. Fresh verification in this slice: `bash -n scripts/deploy-host.sh` and `bash scripts/tests/git-backed-deploy-scripts.sh`.
- Completed the March 19, 2026 git-backed remote-host deployment slice. Fantasma now includes `scripts/bootstrap-host-checkout.sh` to convert a host path into a git-backed checkout while preserving host-local `.env*` files, `scripts/deploy-host.sh` to deploy an explicit commit in detached-HEAD mode and rebuild the `fantasma-prod` / `fantasma-demo` stacks from that pinned revision, and `scripts/tests/git-backed-deploy-scripts.sh` as a cheap shell audit for the new flow. `.env.production`, `.env.demo`, and `.env.local` are now ignored so deployed checkouts can stay clean while secrets remain host-local, and `docs/deployment.md` now documents the bootstrap and pinned-commit deploy workflow. Fresh verification in this slice: `bash -n scripts/bootstrap-host-checkout.sh`, `bash -n scripts/deploy-host.sh`, and `bash scripts/tests/git-backed-deploy-scripts.sh`.
- Completed the March 19, 2026 production Postgres tuning slice for the live single-host deployment. `infra/docker/compose.prod.yaml` now overrides the stock `postgres:17-alpine` runtime with explicit write-oriented defaults for `shared_buffers`, `effective_cache_size`, `maintenance_work_mem`, `wal_buffers`, `max_wal_size`, `min_wal_size`, `checkpoint_timeout`, `checkpoint_completion_target`, `commit_delay`, and `commit_siblings`, while deliberately keeping `fsync`, `full_page_writes`, and `synchronous_commit` on. `docs/deployment.md` now documents the tuned defaults plus the matching `FANTASMA_POSTGRES_*` env escape hatches for per-host adjustment. Fresh verification in this slice: `FANTASMA_ADMIN_TOKEN=test-token docker compose -f infra/docker/compose.yaml -f infra/docker/compose.prod.yaml config`.
- Completed the March 19, 2026 production proxy-network durability follow-up for the live production deployment. Fantasma now ships a production-only Compose override at `infra/docker/compose.prod.yaml` that attaches `fantasma-api` to the shared external `proxy` network with the stable alias `fantasma-api`, so `api.usefantasma.com` no longer depends on a manual `docker network connect` after redeploys. I also added `scripts/tests/prod-proxy-compose.sh` as a cheap config regression and updated the deployment runbook with the two-file production command plus the loopback-port recommendation for shared hosts. Fresh verification in this slice: `FANTASMA_ADMIN_TOKEN=test-token docker compose -f infra/docker/compose.yaml -f infra/docker/compose.prod.yaml config` and `bash scripts/tests/prod-proxy-compose.sh`.
- Completed the March 18, 2026 worker throughput tuning decision: raised the default `session_batch_size` from `1000` to `2000` after your preference to maximize write throughput over readiness. Updated the runtime defaults in `WorkerConfig` and env bootstrap, benchmark default config, both checked-in local and bench Compose stacks, and deployment docs so the default defaults and operator-facing config now align on `FANTASMA_WORKER_SESSION_BATCH_SIZE=2000`.
- Completed a March 17, 2026 review follow-up on the exact-range `active_installs` sessions redesign. The CLI text renderer now emits the approved `dimensions<TAB>start<TAB>end<TAB>value` header for non-JSON `active_installs` output, OpenAPI now splits route-specific granularity parameters so event metrics publish `granularity` as required while session metrics keep the conditional `active_installs` contract plus `interval`, and a new Docker-backed pipeline regression proves exact-range `active_installs` stays visible after a committed session batch even when later raw session work is still pending. Fresh verification in this follow-up: `cargo test -p fantasma-cli --test http_flows metrics_sessions_active_installs_text_renders_exact_range_points -- --exact --nocapture`, `cargo test -p fantasma-api --lib http::tests::openapi_documents_scoped_auth_and_event_metrics_contract -- --exact --nocapture`, and `./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_keeps_active_installs_visible_while_session_backlog_remains -- --exact --nocapture`.
- Started the March 17, 2026 exact-range `active_installs` redesign. This supersedes the earlier same-day aligned-bucket-only direction for public `active_installs`: the corrected plan is to keep `active_installs` under `GET /v1/metrics/sessions`, switch it to exact `start` / `end` semantics with optional `interval=day|week|month|year`, keep the v1 guardrail on max returned points instead of a flat span cap, and back it with one worker-maintained daily bitmap storage model instead of separate week/month/year cuboids. Current handoff docs are [`docs/superpowers/specs/2026-03-17-active-installs-exact-range-design.md`](./superpowers/specs/2026-03-17-active-installs-exact-range-design.md) and [`docs/superpowers/plans/2026-03-17-active-install-range-family.md`](./superpowers/plans/2026-03-17-active-install-range-family.md).
- Completed the March 17, 2026 exact-range `active_installs` redesign. `GET /v1/metrics/sessions?metric=active_installs` now requires exact inclusive UTC `start` and `end`, accepts optional `interval=day|week|month|year`, rejects `granularity`, and counts installs with at least one session whose `session_start` falls inside each exact request window or clipped calendar-shaped interval window. The public response now uses a dedicated exact-range type instead of overloading the old bucketed `MetricsResponse`; the CLI now uses `--start/--end [--interval]` plus the text header `dimensions<TAB>start<TAB>end<TAB>value`; and the API read path now unions worker-maintained daily roaring bitmaps rebuilt from `session_active_install_slices` instead of reading separate week/month/year `active_install_metric_buckets_*` cuboids at runtime. `count`, `duration_total`, and `new_installs` keep their existing bucketed `granularity` behavior. This completion also updates the benchmark harness and operator smoke scripts to use the corrected exact-range contract so the published `reads-visibility` evidence and CLI workflow match the shipped API. Fresh verification in this slice: `cargo fmt --all --check`, `bash -n scripts/cli-smoke.sh scripts/compose-smoke.sh`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test -p fantasma-api --lib -- --nocapture`, `./scripts/docker-test.sh -p fantasma-api --test auth -- --nocapture`, `cargo test -p fantasma-cli --test http_flows -- --nocapture`, `./scripts/docker-test.sh -p fantasma-store active_install_ -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline active_installs_ -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_exposes_bucketed_session_metrics_after_worker_batch -- --exact --nocapture`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_exposes_dim2_session_metrics_and_active_install_overlaps -- --exact --nocapture`, `cargo test -p fantasma-bench session_active_installs_queries_use_exact_range_interval_contract -- --nocapture`, `cargo run -p fantasma-bench -- slo --output-dir artifacts/performance/2026-03-17-active-install-range-family --scenario reads-visibility-30d`, and `./scripts/cli-smoke.sh`. Fresh benchmark evidence: [`artifacts/performance/2026-03-17-active-install-range-family/summary.md`](../artifacts/performance/2026-03-17-active-install-range-family/summary.md) now passes `reads-visibility-30d` with `seed_ingest 23231.91 events/s`, `event_metrics_ready_ms 125`, `session_metrics_ready_ms 60624`, and exact-range `active_installs` query p95s between `0ms` and `4ms` across day/week/month/year visibility-only reads.
- Started the March 17, 2026 `live_installs` endpoint slice. Scope is a deliberate public metrics-model expansion: add a narrow `GET /v1/metrics/live_installs` current-state route beside the two bucketed family endpoints, keep it project-total-only and read-key-backed, and maintain its `(project_id, install_id) -> latest received_at` state asynchronously from the existing `event_metrics` worker lane with bounded indexed reads.
- Completed the March 17, 2026 `live_installs` endpoint slice. The public metrics surface now includes `GET /v1/metrics/live_installs` as a narrow current-state route beside the two bucketed family endpoints, `fantasma-worker` maintains `live_install_state` from raw-event `received_at` inside the existing `event_metrics` lane, the CLI exposes `fantasma metrics live-installs`, and OpenAPI/architecture/deployment docs now describe the expanded contract and operator workflow. Follow-up review hardening switched the live-install `as_of` clock from `transaction_timestamp()` to `statement_timestamp()` so the 120-second cutoff matches the count statement boundary, and `fantasma-store` now has a regression test that delays between `BEGIN` and the count query to lock that in. I also sorted out the CLI smoke portability issue by making `infra/docker/compose.yaml` accept env-driven port mappings and having `./scripts/cli-smoke.sh` override them to ephemeral loopback bindings, then discover the published ingest/API URLs with `docker compose port`, so the disposable smoke stack no longer depends on host `5432`, `8081`, or `8082` being free. Fresh verification in this slice: `cargo fmt --all --check`, `cargo test -p fantasma-core`, `cargo test -p fantasma-api --lib`, `cargo test -p fantasma-cli --test http_flows`, `cargo test -p fantasma-store forward_migration_adds_live_install_state_storage -- --nocapture`, `cargo test -p fantasma-worker live_install_state_updates_keep_latest_received_at_per_install -- --nocapture`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-api --test auth live_installs_route_requires_read_key_auth -- --exact`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-api --test auth live_installs_rejects_public_query_params -- --exact`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-store live_install_counts_use_bounded_read_indexes -- --nocapture`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-store live_install_count -- --nocapture`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker event_worker_updates_live_install_state -- --nocapture`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_exposes_live_installs_after_event_worker_batch -- --exact --nocapture`, `cargo clippy --workspace --all-targets -- -D warnings`, `bash -n scripts/cli-smoke.sh`, `bash -n scripts/tests/cli-smoke-portability.sh`, `bash scripts/tests/cli-smoke-portability.sh`, `FANTASMA_ADMIN_TOKEN=test-token docker compose -f infra/docker/compose.yaml config -q`, and `./scripts/cli-smoke.sh`.
- Closed the March 17, 2026 Timescale spike and decided to stick with Postgres. The runnable branch experiment lives only as notes in `docs/superpowers/`: the discarded alternate backend proved that Fantasma could be bent into a Timescale benchmark path without changing the public API, but the comparable `reads-visibility-30d` run did not produce a compelling win. Final branch-local numbers from the discarded experiment: Postgres seeded `900000` events at `29526.29` events/s with `event_metrics_ready_ms=122` and `session_metrics_ready_ms=256196`, while the Timescale branch seeded the same workload at `23964.82` events/s with `event_metrics_ready_ms=235` and `session_metrics_ready_ms=256547`. Raw-discovery p95s were mixed, not broadly better: Postgres `events_catalog` `171/156/176ms` and `events_top` `150/154/169ms`, versus Timescale `181/150/166ms` and `163/151/162ms`. The implementation itself was discarded; the retained reference is this status note plus the Timescale design/spec docs for future reconsideration.
- Started the March 17, 2026 Timescale raw-discovery feasibility spike. Scope is benchmark-only and stays off the default `fantasma-local` stack: the experiment will add a dedicated Timescale benchmark Compose variant pinned to `timescale/timescaledb:2.25.2-pg17`, extend `fantasma-bench` with raw discovery query coverage plus database-version evidence, and stop immediately if `events_raw` hypertable legality conflicts with the current raw-id ordering assumptions.
- Completed the March 17, 2026 Timescale raw-discovery feasibility harness slice and review hardening. `fantasma-bench slo` now accepts `--database-backend <postgres|timescale>`, the benchmark stack can render either the stock `postgres:17-alpine` template or the pinned `timescale/timescaledb:2.25.2-pg17` variant without touching the default local stack, raw discovery reads for `/v1/metrics/events/catalog` and `/v1/metrics/events/top` are part of the read-visibility matrix with no-filter / first-class-column / property-filter cases, and each `reads-visibility-*` run now writes `database.json` plus `raw-discovery-explain.json` sidecars for feasibility evidence. The Timescale variant now enables `timescaledb` during Postgres initdb via the benchmark Compose stack, `prepare_benchmark_database` fails feasibility immediately if the extension is missing after bootstrap, and raw discovery timing now validates the returned `events` payload shape plus expected row/count totals before recording latency. Fresh verification in this slice: `cargo test -p fantasma-bench --quiet`, `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings`, `FANTASMA_ADMIN_TOKEN=test-token docker compose -f infra/docker/compose.bench.yaml config`, `FANTASMA_ADMIN_TOKEN=test-token docker compose -f infra/docker/compose.bench.timescale.yaml config`, `FANTASMA_ADMIN_TOKEN=test-token cargo run -p fantasma-bench -- slo --output-dir artifacts/performance/2026-03-17-timescaledb-spike-postgres --scenario reads-visibility-30d --database-backend postgres`, and `FANTASMA_ADMIN_TOKEN=test-token cargo run -p fantasma-bench -- slo --output-dir artifacts/performance/2026-03-17-timescaledb-spike-timescale --scenario reads-visibility-30d --database-backend timescale`. Fresh benchmark evidence: the stock Postgres control passed under [`artifacts/performance/2026-03-17-timescaledb-spike-postgres/summary.md`](../artifacts/performance/2026-03-17-timescaledb-spike-postgres/summary.md), with `events_catalog_*` p95s between `156ms` and `177ms`, `events_top_*` p95s between `145ms` and `162ms`, and validated `raw-discovery-explain.json` output for the six raw discovery query classes. The Timescale feasibility run still stopped exactly at the schema-compatibility gate under [`artifacts/performance/2026-03-17-timescaledb-spike-timescale/reads-visibility-30d/database.json`](../artifacts/performance/2026-03-17-timescaledb-spike-timescale/reads-visibility-30d/database.json): `timescaledb_version=2.25.2`, `events_raw_hypertable_ready=false`, and `cannot create a unique index without the column "timestamp" (used in partitioning)`, confirming that the current `id BIGSERIAL PRIMARY KEY` / raw-id-ordering contract blocks a transparent hypertable swap even with bootstrap-time extension setup.
- Superseded the earlier March 17, 2026 range active-installs slice direction. That intermediate aligned-`granularity` / non-daily-public-cuboid design was replaced later the same day by the exact-range `GET /v1/metrics/sessions?metric=active_installs` contract described above, so the exact-range sessions design is the only current public direction for `active_installs`.
- Published the current D2 branch as the Swift SDK `v0.2.0` release on March 17, 2026. The root Swift package tag now matches the two-property ingest contract already enforced by the API, schemas, and iOS client, and the package-facing install example in `sdks/ios/README.md` now points at `0.2.0`.
- Hardened the March 17, 2026 grouped session-read follow-up with reduced regressions after the reads benchmark caught the bug. In addition to the API helper regressions for temporary cuboid skew, the pipeline suite now includes a route-level grouped session catch-up regression and a readiness-poll regression that mirrors the benchmark contract by repeatedly querying grouped session counts until the expected total is visible without ever surfacing `500 internal_server_error`. `AGENTS.md` now also records the repository rule that every correctness bug caught by benchmarks, smoke runs, or manual verification must be reduced to an automated regression in the same change.
- Closed the March 17, 2026 grouped session-read benchmark regression in the unified D2 branch. Root symptom: `reads-visibility-30d` was failing with `500 internal_server_error` on grouped `GET /v1/metrics/sessions?...&group_by=platform&group_by=app_version` during readiness polling. The API now treats temporary cuboid skew during asynchronous catch-up as an incomplete null-synthesis case instead of a fatal invariant, so grouped session reads keep returning visible groups while the worker converges rather than aborting the request. Fresh verification for this follow-up: `cargo test -p fantasma-api synthesize_single_group_counts_tolerates_temporarily_inconsistent_totals -- --nocapture`, `cargo test -p fantasma-api synthesize_two_group_counts_tolerates_temporarily_inconsistent_totals -- --nocapture`, `cargo test -p fantasma-api synthesize_two_group_counts_handles_single_null_buckets_after_filtering -- --nocapture`, and `cargo run -p fantasma-bench -- slo --output-dir artifacts/performance/2026-03-17-unified-d2-reads30-fixed --scenario reads-visibility-30d`. The repaired reads artifact now publishes cleanly with `seed_ingest` `29.53k` events/s, `session_metrics_ready_ms` / `derived_metrics_ready_ms` `32080`, day-level grouped session count p95 `2ms`, hour-level grouped session count p95 `15ms`, and day-level grouped / filtered `active_installs` p95 `6ms` / `5ms`.
- Completed the unified D2 rollback on March 17, 2026. Event ingest, event metrics, session metrics, and the iOS SDK now share the same intentional D2 boundary: event payloads accept at most 2 explicit properties, event/session metric queries accept at most 2 distinct referenced dimensions across filters plus `group_by`, and the bounded bucket storage now stops at `total/dim1/dim2` for both event and session cuboids. The session snapshot columns added during the D4 work remain in place, `new_installs` keeps insert-once first-seen semantics, and `active_installs` stays `granularity=day` with overlap semantics through the existing worker-built `session_active_install_slices` path. Fresh verification for the rollback: `cargo test -p fantasma-core returns_indexed_error_when_more_than_two_properties_are_present -- --nocapture`, `cargo test -p fantasma-api parse_event_metrics_query_rejects_three_referenced_dimensions -- --nocapture`, `cargo test -p fantasma-api parse_session_metric_query_rejects_three_referenced_dimensions -- --nocapture`, `cargo test -p fantasma-api openapi_documents_scoped_auth_and_event_metrics_contract -- --nocapture`, `cargo test -p fantasma-cli --test http_flows -- --nocapture`, `cargo test -p fantasma-bench --quiet`, `cargo test -p fantasma-worker --test pipeline --no-run`, `cargo test -p fantasma-core -p fantasma-api -p fantasma-store -p fantasma-worker -p fantasma-cli -p fantasma-bench --no-run`, `./scripts/docker-test.sh -p fantasma-store run_migrations_create_bucketed_metrics_and_first_seen_tables -- --nocapture`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-store run_migrations_creates_session_active_install_slices_table -- --nocapture`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_ -- --nocapture`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `bash -n scripts/cli-smoke.sh scripts/compose-smoke.sh`, and `swift test --filter FantasmaSDKTests`. Fresh benchmark evidence for the D2 branch is now published under `artifacts/performance/2026-03-17-unified-d2-representative`, `artifacts/performance/2026-03-17-unified-d2-mixed`, and `artifacts/performance/2026-03-17-unified-d2-reads30-fixed`, with representative and mixed both back to `flat` versus the March 15 baseline and the reads suite now publishing cleanly instead of failing on grouped session reads.
- Reworked the March 17, 2026 session-D4 hot path after the first benchmark regression. Incremental session writes no longer maintain session metric cuboids inline; they now enqueue touched day/hour buckets for the shared finalizer, while repair/finalization keeps the set-based SQL rebuild path for `session_metric_buckets_*`. `active_installs` grouping stayed supported, but the API now reads worker-built `session_active_install_slices` instead of aggregating from `sessions` on demand. The recovery also fixed two correctness bugs that showed up once the derived slice path was exercised against late-event coverage: nullable `app_version` / `os_version` snapshots now use explicit null flags plus stored sentinel values in `session_active_install_slices`, and the set-based session cube rebuild no longer emits null built-in dimensions into dim tables that still rely on synthesized null groups. A follow-up worker fix serialized shared bucket drains and removed a leftover inline day-bucket rebuild from the append path after the representative bench exposed repeated `session_metric_buckets_total` deadlocks under concurrent repair/finalization.
- Fresh verification for the hot-path recovery slice: `cargo test -p fantasma-worker --lib --no-run`, `cargo test -p fantasma-api parse_session_metric_query -- --nocapture`, `cargo test -p fantasma-api openapi_documents_scoped_auth_and_event_metrics_contract -- --nocapture`, `cargo test -p fantasma-cli --test http_flows -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker replayed_append_batch_only_advances_offset_after_coordinator_failure -- --nocapture`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker replayed_repair_enqueue_only_advances_raw_offset_after_coordinator_failure -- --nocapture`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_exposes_dim4_session_metrics_and_active_install_overlaps -- --exact`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_keeps_new_installs_fixed_on_first_seen_bucket_after_late_events -- --exact`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_moves_grouped_session_app_version_after_late_event_repair -- --exact`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-store run_migrations_create_bucketed_metrics_and_first_seen_tables -- --nocapture`, and `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-store run_migrations_creates_session_active_install_slices_table -- --nocapture`.
- Fresh benchmark evidence for the recovery slice: `cargo run -p fantasma-bench -- slo --output-dir artifacts/performance/2026-03-17-session-d4-recovery-representative --scenario live-append-small-blobs`, `cargo run -p fantasma-bench -- slo --output-dir artifacts/performance/2026-03-17-session-d4-recovery-mixed --scenario live-append-plus-light-repair`, and `cargo run -p fantasma-bench -- slo --output-dir artifacts/performance/2026-03-17-session-d4-recovery-reads30 --scenario reads-visibility-30d`. The recovery materially improved the first-pass regression but did not clear the retained March 15 iterative baseline: representative append landed at `1059.40` events/s versus baseline `2201.76` (`51.9%` slower, improved from the first pass `965.50`), mixed append landed at `1105.55` events/s versus baseline `2214.65` (`50.1%` slower, improved from `987.37`), and repair uploads in the mixed scenario still took `42189ms` for `180` events (`4.27` events/s). Final derived readiness stayed small (`9ms` representative, `19ms` mixed), but checkpoint session / derived p95 remained elevated at `1485ms` representative and `1784ms` mixed. The recovered `reads-visibility-30d` run seeded `900000` events at `27.24k` events/s, reached session / derived readiness in `178797ms`, and kept grouped read latency bounded: day-level grouped session D4 reads stayed at `3ms` to `4ms` p95, hour-level dim4 grouped session reads stayed at `25ms` p95, and day-level dim4 `active_installs` ungrouped / grouped / filtered reads were `4ms`, `6ms`, and `3ms` p95 respectively. Current verdict remains `worse` against the March 15 baseline, so this branch is functionally corrected and less regressed than the first pass, but it is not yet back within the prior append throughput envelope.
- Recorded the ClickHouse benchmark conclusion on March 16, 2026 and cleaned the spike code back out of the tree. The saved evidence stays in `/tmp/fantasma-bench-postgres/summary.json`, `/tmp/fantasma-bench-clickhouse-small-v4/summary.json`, and `/tmp/fantasma-bench-clickhouse-repair-v1/summary.json`: versus the frozen Postgres baseline, ClickHouse was slower on the dominant append path in both representative iterative scenarios by about `35%` (`1439.18` vs `2201.76` events/s on `live-append-small-blobs`, `1432.00` vs `2214.65` events/s on `live-append-plus-light-repair`). ClickHouse only won on the tiny `180`-event repair upload phase (`142ms` vs `6495ms`). Current repository direction therefore remains Postgres-only v1 unless a future spike revisits ClickHouse with a materially different public ingest contract or benchmark shape.
- Completed session-metric D4 and session-scoped property support on March 17, 2026. `sessions` and `install_first_seen` now snapshot `platform`, `app_version`, `os_version`, and explicit properties from the first event, the worker maintains session metric cuboids through dim4 for `count`, `duration_total`, and `new_installs`, and `GET /v1/metrics/sessions` now accepts the same D4/property filter and `group_by` vocabulary as event metrics. `active_installs` remains `granularity=day`, but it now supports filtered and grouped D4 session reads with overlap semantics, so filtered/grouped counts can legitimately exceed the unfiltered distinct-install total. CLI/OpenAPI/architecture/deployment/performance docs were updated in the same slice. Fresh verification for this slice: `cargo test -p fantasma-worker sessionization -- --nocapture`, `cargo test -p fantasma-api parse_session_metric_query -- --nocapture`, `cargo test -p fantasma-api openapi_documents_scoped_auth_and_event_metrics_contract -- --nocapture`, `cargo test -p fantasma-cli --test http_flows -- --nocapture`, `cargo test -p fantasma-bench --quiet`, `cargo test -p fantasma-store forward_migration_adds_session_dim4_storage_and_snapshot_columns -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_exposes_dim4_session_metrics_and_active_install_overlaps -- --exact`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_keeps_new_installs_fixed_on_first_seen_bucket_after_late_events -- --exact`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_moves_grouped_session_app_version_after_late_event_repair -- --exact`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-store session_metric_bucket_dim -- --nocapture`, and `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-store run_migrations_create_bucketed_metrics_and_first_seen_tables -- --nocapture`. Clean benchmark evidence: `cargo run -p fantasma-bench -- slo --output-dir artifacts/performance/2026-03-17-session-d4-representative --scenario live-append-small-blobs`, `cargo run -p fantasma-bench -- slo --output-dir artifacts/performance/2026-03-17-session-d4-mixed --scenario live-append-plus-light-repair`, and `cargo run -p fantasma-bench -- slo --output-dir artifacts/performance/2026-03-17-session-d4-reads30 --scenario reads-visibility-30d`. Results versus the retained March 15 iterative baseline were slower on append throughput by about `56%` (`965.50` vs `2201.76` events/s on `live-append-small-blobs`, `987.37` vs `2214.65` events/s on `live-append-plus-light-repair`), while final representative/mixed derived readiness still landed at `12ms` and checkpoint session p95 stayed around `1.6s` (`1612ms` representative, `1594ms` mixed). The clean `reads-visibility-30d` run seeded `900000` events at `25.12k` events/s, reached session/derived readiness in `166309ms`, and kept grouped session D4 reads bounded: p95 `3ms` for day-level grouped `count`, `duration_total`, and `new_installs`, p95 `25ms` to `26ms` for the hour-level dim4 grouped variants, and p95 `10ms` / `5ms` for day-level dim4 grouped / filtered `active_installs`.
- Completed the Swift SDK CI flake fix on March 16, 2026. Root cause: GitHub Actions run `23154065298` failed in `destinationDriftDuringBatchAssemblyDiscardsPreparedBatch` because the test released the upload boundary after the reconfigure task was scheduled but before the actor had necessarily recorded `pendingReconfiguration`, so the old batch could still upload first on slower runners. The fix keeps public SDK behavior unchanged, adds an internal-only testing probe for the pending configuration state, and makes that test wait for the actual pending destination before releasing the upload gate. Fresh verification for this slice: `swift test --filter destinationDriftDuringBatchAssemblyDiscardsPreparedBatch`, `for i in {1..20}; do swift test --filter destinationDriftDuringBatchAssemblyDiscardsPreparedBatch >/tmp/fantasma-swift-flake.log 2>&1 || { cat /tmp/fantasma-swift-flake.log; exit 1; }; done`, and `swift test -Xswiftc -strict-concurrency=complete`.
- Completed Swift SDK Swift 5.6 package migration on March 16, 2026. The root manifest remains `// swift-tools-version: 5.6`, while iOS SDK Swift Testing coverage is compiler-gated (`#if swift(>=6.0)`) so consumers can use the package from Swift 5.6 without `any` existential syntax or testing framework incompatibilities.
- Completed the root-package Swift SDK distribution prep on March 16, 2026. The Swift package is now authored at the repository root (`Package.swift`) with `FantasmaSDK` as the only public package product, the nested `sdks/ios/FantasmaSDK/Package.swift` manifest was removed, and CI now runs Swift checks from the repo root. `sdks/ios/README.md` now documents root-package installation for users plus a local-path fallback for development.
- Completed Docker disk-first local workflow cleanup on March 15, 2026. Fantasma's local Docker workflows now default to aggressive cleanup: the main, bench, and Docker-test Compose files declare stable default project names (`fantasma-local`, `fantasma-bench`, and `fantasma-tests`), `./scripts/compose-smoke.sh` and `./scripts/cli-smoke.sh` both pre-clean and tear down with `--volumes --remove-orphans --rmi local`, `./scripts/docker-test.sh` drops cache volumes and local images unless `FANTASMA_DOCKER_TEST_KEEP_CACHE=1`, the smoke helpers keep explicit `FANTASMA_SMOKE_KEEP_STACK=1` and `FANTASMA_CLI_SMOKE_KEEP_STACK=1` escape hatches for debugging, and `./scripts/docker-reclaim.sh` removes older Fantasma-owned Docker artifacts already on disk. Fresh verification for this slice: `bash scripts/tests/docker-disk-footprint.sh`, `bash -n scripts/docker-test.sh scripts/compose-smoke.sh scripts/cli-smoke.sh scripts/docker-reclaim.sh`, `FANTASMA_ADMIN_TOKEN=test-token docker compose -f infra/docker/compose.yaml config -q`, `docker compose -f infra/docker/compose.test.yaml config -q`, and `FANTASMA_ADMIN_TOKEN=test-token docker compose -f infra/docker/compose.bench.yaml config -q`. Outcome: repeated local smoke and Docker-test runs now return disk space by default instead of steadily accumulating Fantasma-owned images, volumes, and Compose leftovers.
- Completed CLI event discovery coverage on March 15, 2026. `fantasma-cli` now exposes the remaining read-key-backed public event reads through `metrics events-total`, `metrics events-top`, and `metrics events-catalog` while preserving the existing grouped `metrics events` series command. Fresh verification for this slice: `cargo test -p fantasma-cli --test http_flows`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `./scripts/docker-test.sh`, and `git diff --check`. Outcome: the operator CLI now covers the same event read surface that the product relies on instead of leaving those routes as raw-HTTP-only gaps.
- Completed low-severity dead-flow cleanup on March 15, 2026. Fantasma no longer exports unused pool-backed store wrappers that had no repo callers, `docs/deployment.md` now correctly states that `./scripts/cli-smoke.sh` runs in its own disposable Compose stack with a fresh `XDG_CONFIG_HOME`, and the lower-level `./scripts/compose-smoke.sh` path is now documented as optional manual smoke instead of the primary operator dogfooding path. Fresh verification for this slice: `bash scripts/tests/dead-flow-audit.sh`, `bash -n scripts/tests/dead-flow-audit.sh`, `cargo clippy -p fantasma-store --lib -- -D warnings`, and `cargo clippy -p fantasma-worker --lib -- -D warnings`. Outcome: the repo no longer carries these stale store/doc dead ends as live product surface.
- Completed total event volume metrics on March 15, 2026. Fantasma now exposes read-key-authenticated `GET /v1/metrics/events/total`, which returns one bucketed total-event series for the selected window using the same public time-range and filter vocabulary as the existing event metrics family. Fresh verification for this slice: `cargo test -p fantasma-api openapi_documents_scoped_auth_and_event_metrics_contract --lib`, `./scripts/docker-test.sh -p fantasma-api --test auth`, and `./scripts/docker-test.sh -p fantasma-worker --test pipeline`. Outcome: downstream clients can read truthful project-level total event volume through the public API without storage backdoors.
- Completed CLI-first operator dogfooding enforcement on March 15, 2026. Fantasma now treats `fantasma-cli` as the required operator-facing development path in contributor guidance and deployment docs, CI computes changed-file scope through `scripts/ci/should-run-cli-smoke.sh`, and relevant backend/operator changes now trigger a dedicated `./scripts/cli-smoke.sh` gate instead of relying only on lower-level smoke or raw HTTP checks. Fresh verification for this slice: `bash scripts/tests/cli-dogfood-gate.sh`, `bash -n scripts/cli-smoke.sh scripts/provision-project.sh scripts/ci/should-run-cli-smoke.sh scripts/tests/cli-dogfood-gate.sh`, `./scripts/cli-smoke.sh`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `docker compose -f infra/docker/compose.yaml config -q`.
- Completed public project metadata and event discovery routes on March 15, 2026. Fantasma now exposes operator-authenticated `GET /v1/projects/{project_id}` and `PATCH /v1/projects/{project_id}` plus read-key-authenticated `GET /v1/metrics/events/catalog` and `GET /v1/metrics/events/top`, with matching OpenAPI, auth regression coverage, and real-ingest pipeline coverage. Fresh verification for this slice: `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `./scripts/docker-test.sh -p fantasma-api --test auth`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline`, and `git diff --check`. Outcome: external clients can use public project metadata reads/edits and lightweight event discovery without any storage backdoor or product-specific concepts in Fantasma.
- Completed security and ingest hardening on March 15, 2026. Fantasma now fails closed unless `FANTASMA_ADMIN_TOKEN` is set in active runtime paths, local smoke scripts mint an ephemeral operator token when one is not provided, `scripts/provision-project.sh` refuses to run without an explicit bearer token, `fantasma-ingest` now has direct crate-local HTTP coverage for auth, malformed JSON, payload limits, validation envelopes, and happy-path ingestion, and benchmark publication now emits only coarse `environment` metadata while checked-in performance artifacts, Markdown summaries, and the iterative baseline no longer publish detailed host fingerprints or run timestamps. Fresh verification for this hardening: `bash -n scripts/compose-smoke.sh scripts/cli-smoke.sh scripts/provision-project.sh`, `FANTASMA_ADMIN_TOKEN=test-token docker compose -f infra/docker/compose.yaml config -q`, `FANTASMA_ADMIN_TOKEN=test-token docker compose -f infra/docker/compose.bench.yaml config -q`, `cargo fmt --all --check`, `cargo test -p fantasma-api load_authorizer_from_env --bin fantasma-api`, `./scripts/docker-test.sh -p fantasma-ingest --test http`, `cargo test -p fantasma-bench --quiet`, and `cargo clippy --workspace --all-targets -- -D warnings`. Repo audit follow-up: no active docs, infra, scripts, bench code, or checked-in performance artifacts still expose the old fixed operator-token fallback or detailed benchmark host fingerprints.
- Completed the repo-scope cleanup and tidiness audit on March 15, 2026. Fantasma now presents this repository as API/CLI and SDK only: the obsolete placeholder client and its Compose wiring are gone, `README.md`, `docs/deployment.md`, `docs/architecture.md`, and `AGENTS.md` no longer position an extra bundled client as in-scope, and stale read-key fixture names were renamed to `"local-read"`. Fresh verification for this cleanup: `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `docker compose -f infra/docker/compose.yaml config -q`, `./scripts/docker-test.sh -p fantasma-api --test auth`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline`, `./scripts/docker-test.sh -p fantasma-store api_key_lifecycle_round_trips_kinds_and_revocation`, and `./scripts/docker-test.sh`. Audit result: the main untidiness was stale scope language plus the shipped placeholder client; after cleanup, no active repo docs, infra, apps, scripts, or crate sources still use that old positioning.
- Active append-only optimization loop on March 15, 2026: targeting `live-append-small-blobs` iterative as the primary append freshness signal, starting from the accepted local baseline `runtime 184.42s`, `checkpoint derived p95 327ms`, and `final derived readiness 8ms`. This slice is benchmark-owned and append-only: add checkpoint-target append attribution first, use benchmark-side DB probing only because the bench stack is single-project and scenario-isolated, then run a bounded A/B/C hypothesis ladder and keep only materially better append states while preserving final derived readiness and correctness. STATUS updates in this loop must record the baseline plus each hypothesis with touched subsystem/helper, checkpoint derived `p50/p95`, final derived readiness, and keep/revert outcome.
- Standing worker-tuning principle as of March 15, 2026: favor representative append freshness over backfill throughput when choosing default worker settings. Fantasma's intended traffic is durable SDK-driven mobile append traffic, not operator-driven historical replay. The official iOS SDK persists events in a FIFO SQLite queue, stamps timestamps at enqueue time, and flushes sequentially, so normal client traffic should be append-dominant with very low repair pressure. Treat repair as a bounded guardrail path and backfill as a non-default/operator-only path. A worker-default candidate may be kept if it materially improves representative append freshness while keeping mixed-repair behavior bounded and operationally safe; backfill regressions alone should not block promotion. Revisit this policy only if Fantasma adds client-supplied timestamps, historical import behavior, or other first-class sources of normal-path out-of-order traffic.
- Append attribution baseline on March 15, 2026 touched `crates/fantasma-bench/src/main.rs` plus the bench compose wiring to record checkpoint-target append attribution from benchmark-side DB probing. The first representative append run with the new sidecar came back at `checkpoint derived p50 319ms`, `checkpoint derived p95 327ms`, and `final derived readiness 10ms`; attribution split that run into `upload->event lane p50/p95 311/315ms`, `upload->session lane p50/p95 207/312ms`, and `latest lane->derived visibility p50/p95 8/14ms`, with `upload_to_event_lane_ready` dominant. Final kept-state verification finished at `checkpoint derived p50 319ms`, `checkpoint derived p95 325ms`, and `final derived readiness 8ms`. Result: kept as the append attribution baseline.
- Append Hypothesis A sensitivity on March 15, 2026 touched the bench-only worker override path in `crates/fantasma-bench/src/main.rs` and `infra/docker/compose.bench.yaml` to allow conditional `idle_poll_interval_ms` sweeps after attribution justified it. Running `live-append-small-blobs` with `worker_idle_poll_interval_ms=75` produced `checkpoint derived p50 216ms`, `checkpoint derived p95 316ms`, and `final derived readiness 10ms`; the event-lane median improved sharply, but p95 only moved by `11ms`, below the material keep threshold. Result: reverted as a performance baseline and kept only as benchmark instrumentation.
- Append Hypothesis A scheduler candidate on March 15, 2026 touched `crates/fantasma-worker/src/scheduler.rs` to skip idle sleep after a no-progress tick when backlog remained visible. Focused scheduler tests passed, but the representative append reruns landed at `checkpoint derived p50/p95 233/335ms` and `231/336ms` with `final derived readiness 10ms` and `12ms`, so the tail did not improve materially and the distribution stayed event-lane bound. Result: reverted; the remaining append bottleneck for this loop is event-lane catch-up, not derived-visibility lag or session/apply churn.
- Event-lane attribution follow-up on March 15, 2026 touched `crates/fantasma-worker/src/worker.rs` and `crates/fantasma-bench/src/main.rs` to keep the append attribution baseline but add minimal per-batch `event_metrics` trace coverage: `build_rollups`, per-table `upsert_*`, `save_offset`, `commit`, plus delta-row counts by cuboid family. The bench append sidecar now persists the raw `event-lane-trace.jsonl` worker trace and embeds an `event_lane_summary` alongside `append-attribution.json`. The first representative append run with that tooling landed at `checkpoint derived p50/p95 318/337ms`, `final derived 7ms`, and showed `upsert_dim4` dominant at `41.04%` of measured event-lane time, followed by `upsert_dim3` at `30.58%`, while `build_rollups` was only `14.50%`; delta-row totals were `dim4 386_271`, `dim3 350_998`, `dim2 156_826`, `dim1 32_551`, `total 2_389`. Result: kept as the event-lane attribution baseline.
- Event-lane Hypothesis A on March 15, 2026 touched `upsert_event_metric_buckets_dim3_in_tx()` and `upsert_event_metric_buckets_dim4_in_tx()` in `crates/fantasma-store/src/lib.rs`, replacing the dominant dim3/dim4 `VALUES` builders with grouped `UNNEST` upserts to collapse intra-statement duplicates and keep fixed-shape SQL on the hottest append write families. Focused store coverage passed on the repository Docker Postgres path (`event_metric_dim3_conflicts_accumulate_across_chunk_boundaries`, `event_metric_dim4_conflicts_accumulate_across_chunk_boundaries`, and `event_metric_dim4_upserts_handle_batches_above_postgres_bind_limit`), but the representative append reruns came back at `checkpoint derived p50/p95 321/328ms` and `321/329ms` with `final derived 11ms` and `9ms`. Event-lane measured time moved to `41.9s` on the first run but regressed to `44.9s` on rerun versus the attribution baseline `44.0s`, so the candidate never cleared the keep bar. Result: reverted.
- Event-lane Hypothesis B diagnostic on March 15, 2026 used the existing bench-only `worker_event_batch_size` override to raise the event lane cap from `5_000` to `20_000` without changing repo defaults. That run landed at `checkpoint derived p50/p95 320/327ms` with `final derived 10ms`, while event-lane measured time dropped to `40.9s` and trace record count fell from `345` to `312`. Result: reverted as a baseline because append p95 stayed effectively flat versus the kept `325ms` target. The useful diagnosis is that simple batch-cap growth trims some internal event-lane work but does not materially move append freshness; the remaining bottleneck is still dominant dim3/dim4 event-lane write work.
- Active split-worker follow-up on March 14, 2026: running an overnight hypothesis ladder strictly on `session_daily_installs_write` inside `shared_bucket_finalize`. Owned files for the ladder are `crates/fantasma-store/src/lib.rs`, `crates/fantasma-worker/src/worker.rs`, `crates/fantasma-bench/src/main.rs`, and `docs/STATUS.md`; only a narrowly justified internal migration is allowed beyond that. Starting accepted baseline for the ladder is the current attribution-reset candidate: `append-30d 22_731ms`, `backfill-30d 111_112ms`, `repair-30d 105_433ms`, with `session_daily_installs_write` dominant at `89.87%` in `backfill-30d` and `85.80%` in `repair-30d`. The ladder order is A) reduce write amplification, B) reduce statement/round-trip fan-out, C) installs-only shared source, D) scan-path/index fix only if A-C fail and focused query evidence justifies it. Each hypothesis must run focused correctness checks plus the exact `30d` trio, then either become the new accepted baseline or be selectively reverted; the repo must end in the best surviving correct state, not the last attempted state.
- Active Hypothesis B retry on March 15, 2026: the keep/revert rule is corrected for this local bottleneck slice. Promotion now depends on both `backfill-30d` and `repair-30d` improving by at least `max(1000ms, 5%)` versus the current kept baseline and on absolute `session_daily_installs_write` `total_ms` dropping in both rebuild-style sidecars. Phase share remains context only; `session_daily_installs_write` does not need to stop being dominant for a candidate to be kept.
- Overnight baseline confirmation run on March 14, 2026 stayed close to the accepted baseline and kept it in force: `append-30d 23_059ms`, `backfill-30d 109_825ms`, `repair-30d 104_868ms`.
- Overnight Hypothesis A on March 14, 2026 touched `rebuild_session_daily_installs_days_in_tx()` in `crates/fantasma-store/src/lib.rs`, switching the touched-scope rebuild to delete only disappeared rows and upsert only new/changed rows. Focused correctness coverage passed (`rebuild_session_daily_days_in_tx_preserves_unchanged_install_rows_within_touched_scope`, `rebuild_session_daily_days_in_tx_leaves_untouched_days_alone`, `rebuild_session_daily_days_in_tx_rollback_preserves_existing_rows`, `rebuild_finalization_sweeps_visible_project_queue_in_one_pass`, and `pipeline_keeps_grouped_session_app_version_fixed_after_late_event_repair`), and the required `30d` trio completed with `append-30d 23_121ms`, `backfill-30d 112_938ms`, and `repair-30d 69_534ms`. Result: reverted. `repair-30d` improved materially, but `backfill-30d` did not improve and `session_daily_installs_write` share worsened in both rebuild-style scenarios to `97.15%` for `backfill-30d` and `93.72%` for `repair-30d`, so the candidate failed the overnight keep rule and did not become the new accepted baseline.
- Overnight Hypothesis B on March 14, 2026 touched `rebuild_session_daily_installs_days_in_tx()` in `crates/fantasma-store/src/lib.rs`, collapsing the touched-scope `session_daily_installs` replace path into one SQL statement with a `fresh_rows` CTE, `deleted_missing` delete, and `ON CONFLICT` upsert. Focused correctness coverage passed (`rebuild_session_daily_days_in_tx_leaves_untouched_days_alone`, `rebuild_session_daily_days_in_tx_rollback_preserves_existing_rows`, `rebuild_session_daily_days_helper_matches_attribution_reset_shape`, `rebuild_session_daily_installs_days_helper_uses_single_replace_statement`, `rebuild_finalization_sweeps_visible_project_queue_in_one_pass`, and `pipeline_keeps_grouped_session_app_version_fixed_after_late_event_repair`), and the required `30d` trio completed with `append-30d 3_227ms`, `backfill-30d 102_380ms`, and `repair-30d 100_179ms`. Result: reverted. `repair-30d` improved versus the accepted baseline, but `backfill-30d` was only marginally better while `session_daily_installs_write` remained dominant and worsened in `backfill-30d` to `94.70%`; `repair-30d` stayed dominant at `86.29%`. The candidate failed the overnight keep rule because the share bar did not improve materially in both scenarios and the readiness shape did not justify promoting it to the accepted baseline.
- Hypothesis B rerun on March 15, 2026 re-applied the same one-statement touched-scope `session_daily_installs_write` path in `rebuild_session_daily_installs_days_in_tx()` and re-ran the focused store/worker checks plus the required `30d` trio under the corrected keep rule. Focused correctness coverage passed (`rebuild_session_daily_days_helper_matches_attribution_reset_shape`, `rebuild_session_daily_installs_days_helper_uses_one_statement_touched_scope_replace`, `rebuild_session_daily_days_in_tx_leaves_untouched_days_alone`, `rebuild_session_daily_days_in_tx_rollback_preserves_existing_rows`, `rebuild_finalization_sweeps_visible_project_queue_in_one_pass`, and `pipeline_keeps_grouped_session_app_version_fixed_after_late_event_repair`), but the benchmark result did not reproduce the earlier promising run: `append-30d 23_030ms`, `backfill-30d 111_346ms`, and `repair-30d 115_420ms`. Absolute `session_daily_installs_write` totals also worsened versus the accepted baseline to `94_799ms` in `backfill-30d` and `76_676ms` in `repair-30d`, with shares `94.86%` and `93.57%`. Result: reverted and stop. The earlier `append-30d 3_227ms` run was likely measurement noise or an unstable transient, and the B shape is not trustworthy enough to promote as a working baseline.
- Current overnight state on March 14, 2026: no hypothesis has been kept yet. The repo has been restored to the starting accepted baseline after reverting both A and B, and the best surviving state remains `append-30d 22_731ms`, `backfill-30d 111_112ms`, `repair-30d 105_433ms` with `session_daily_installs_write` still dominant at `89.87%` / `85.80%`.
- Tracking follow-up work on top of the operator-managed auth flow: any future management surface outside this repository should build on the new `/v1/projects*` management API and scoped `ingest` / `read` project keys instead of reintroducing startup-seeded credentials.

## Engineering Ethos

- Fantasma prefers simple, explicit, high-performance data paths.
- We avoid steady-state designs that rely on broad recomputation.
- We narrow feature shape when necessary to preserve predictable performance and operational clarity.
- Correctness still matters, but we should choose correctness models that compose with incremental processing.

## Completed

- Completed the narrow default-policy follow-up for `5000 / 5000 / 8 / 4` on March 15, 2026 after the final 5k-centered sweep suggested that tuple might be worth a mixed-repair-weighted confirmation. Fresh preflight in this pass: `cargo test -p fantasma-bench --quiet` and `docker compose -f infra/docker/compose.bench.yaml config`. Fresh confirmation evidence in this pass: two paired iterative reruns under `/tmp/fantasma-bench-default-sweep-5000-5000-8-4-r1` and `/tmp/fantasma-bench-default-sweep-5000-5000-8-4-r2` using `cargo run -p fantasma-bench -- slo --output-dir <dir> --scenario live-append-small-blobs --scenario live-append-plus-light-repair --worker-session-batch-size 5000 --worker-event-batch-size 5000 --worker-session-incremental-concurrency 8 --worker-session-repair-concurrency 4`. Both reruns preserved hard-gate correctness, the pinned iterative sample counts (`225` append and `234` mixed repair), `Budget: PASS`, and bounded final derived readiness (`11ms` / `8ms` in `r1`, `10ms` / `8ms` in `r2`). The revised rule still failed on append stability: representative append checkpoint-derived p95 landed at `330ms` in `r1` and `335ms` in `r2`, both worse than the current accepted append baseline `327ms`. Mixed repair stayed materially better at `426ms` and `425ms` versus the accepted `429ms`, but that guardrail win is not enough when the confirmation reruns lose the append-flat requirement. Outcome: do not promote `5000 / 5000 / 8 / 4`, keep all checked-in defaults at `1000 / 5000 / 8 / 2`, leave the pinned iterative baseline unchanged, and stop tuple tuning again.
- Completed the final 5k-centered worker default sweep on March 15, 2026 using only the real iterative default pair `live-append-small-blobs` and `live-append-plus-light-repair`, with one disposable output dir per tuple under `/tmp/fantasma-bench-default-sweep/`. Fresh preflight in this pass: `cargo test -p fantasma-bench --quiet` and `docker compose -f infra/docker/compose.bench.yaml config`. Fresh screen runs in this pass: `cargo run -p fantasma-bench -- slo --output-dir /tmp/fantasma-bench-default-sweep/<tuple> --scenario live-append-small-blobs --scenario live-append-plus-light-repair --worker-session-batch-size <session_batch_size> --worker-event-batch-size <event_batch_size> --worker-session-incremental-concurrency <session_incremental_concurrency> --worker-session-repair-concurrency <session_repair_concurrency>` for all seven planned tuples: `1000 / 5000 / 8 / 2`, `5000 / 5000 / 8 / 2`, `5000 / 7000 / 8 / 2`, `5000 / 5000 / 12 / 2`, `5000 / 5000 / 8 / 4`, `5000 / 7000 / 12 / 2`, and `5000 / 7000 / 8 / 4`. All seven runs preserved hard-gate correctness and the expected checkpoint sample counts (`225` append, `234` mixed repair), all seven scenario pairs published `Budget: PASS`, and final derived readiness stayed bounded at `7ms` to `12ms` for append and `7ms` to `13ms` for mixed repair. The decisive result is flat append tails: representative append checkpoint-derived p95 landed at `324ms`, `334ms`, `328ms`, `333ms`, `324ms`, `327ms`, and `331ms` across the seven tuples, so none cleared the required `<= 310ms` append bar and there were no eligible tuples to rerun. Mixed-repair checkpoint-derived p95 stayed operationally safe at `422ms`, `337ms`, `342ms`, `426ms`, `332ms`, `338ms`, and `336ms`, but that guardrail does not matter without an append win. Outcome: keep the checked-in defaults at `1000 / 5000 / 8 / 2`, do not update worker/Compose/bench/doc defaults or the pinned iterative baseline, and treat 5k-centered worker-tuple tuning as flat under the real gate. Future work should move to product-shape or freshness-semantics changes rather than more default-sweep tuple iteration.
- Reverted the dim3/dim4 temp-staged member-resolution rewrite on March 15, 2026 after full verification and two paired iterative benchmark runs. The candidate had added forward migration `0015_event_metric_dim3_dim4_members.sql`, project-scoped dim3/dim4 member tables, narrow `member_id` bucket storage, temp-staged member resolution, and explicit worker trace phases `resolve_dim3_members` / `upsert_dim3_buckets` / `resolve_dim4_members` / `upsert_dim4_buckets`; correctness verification passed on the repository Docker/Postgres path (`./scripts/docker-test.sh -p fantasma-store forward_migration_rewrites_dim3_dim4_buckets_behind_member_tables -- --quiet`, `./scripts/docker-test.sh -p fantasma-store --lib`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_exposes_event_metrics_after_worker_batch -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_exposes_dim4_event_metrics_with_null_buckets -- --nocapture`) and on the required host checks (`cargo test -p fantasma-bench --quiet`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `docker compose -f infra/docker/compose.bench.yaml config`, and `git diff --check`). The paired iterative confirmation runs used `cargo run -p fantasma-bench -- slo --output-dir /tmp/fantasma-bench-temp-staged-member-r1 --scenario live-append-small-blobs --scenario live-append-plus-light-repair` and `cargo run -p fantasma-bench -- slo --output-dir /tmp/fantasma-bench-temp-staged-member-r2 --scenario live-append-small-blobs --scenario live-append-plus-light-repair`; both preserved the required checkpoint sample counts (`225` append, `234` mixed repair) and landed at `live-append-small-blobs checkpoint derived p95 322ms` with final derived readiness `6ms` and `8ms`, while `live-append-plus-light-repair` landed at `checkpoint derived p95 327ms` with final derived readiness `9ms` and `8ms`. That stayed above the `<= 310ms` keep bar for representative append, so the code changes were backed out and the repository returned to the prior contract/storage shape. Result: this last serious same-contract storage attempt stayed flat; treat the remaining win as a product-shape narrowing problem rather than another internal storage trick.
- Completed the representative default-selection screen for the old `5000 / 5000 / 8 / 2` worker tuple on March 15, 2026 after reverting the failed member-indirection hypothesis. Under the standing SDK-driven append policy, the only scenarios run in this screen were `live-append-small-blobs` and `live-append-plus-light-repair`, using `cargo run -p fantasma-bench -- slo --output-dir /tmp/fantasma-bench-5000-5000-8-2 --scenario live-append-small-blobs --scenario live-append-plus-light-repair --worker-session-batch-size 5000 --worker-event-batch-size 5000 --worker-session-incremental-concurrency 8 --worker-session-repair-concurrency 2`. Both scenarios preserved the pinned iterative sample shapes (`225` append checkpoints and `234` mixed-repair checkpoints) and stayed within budget, but the tuple did not materially improve the metrics that now matter for product-default selection: `live-append-small-blobs` landed at `checkpoint derived p95 323ms` with `final derived readiness 7ms`, versus the kept representative baseline `327ms` / `8ms`, and `live-append-plus-light-repair` landed at `checkpoint derived p95 424ms` with `final derived readiness 7ms`, versus the kept mixed-repair baseline `429ms` / `7ms`. Result: the tuple looks operationally safe enough under the SDK-driven traffic model, but the append win is too small to justify changing defaults, so the checked-in worker defaults remain unchanged.
- Reverted the event-lane dim3/dim4 member-indirection rewrite on March 15, 2026 after recording the result. The candidate had narrowed `event_metric_buckets_dim3` / `dim4` behind new member tables and split the worker trace into `resolve_dim3_members`, `upsert_dim3_buckets`, `resolve_dim4_members`, and `upsert_dim4_buckets`; correctness verification had passed (`cargo test -p fantasma-store forward_migration_rewrites_dim3_dim4_buckets_behind_member_tables --quiet`, `cargo test -p fantasma-store -p fantasma-worker -p fantasma-bench --no-run`, `./scripts/docker-test.sh -p fantasma-store --lib`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_exposes_event_metrics_after_worker_batch -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_exposes_dim4_event_metrics_with_null_buckets -- --nocapture`, `cargo test -p fantasma-worker event_lane_trace_writer_emits_jsonl_when_trace_path_is_set --quiet`, `cargo test -p fantasma-bench --quiet`, `docker compose -f infra/docker/compose.bench.yaml config`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`), but two clean representative reruns with `cargo run -p fantasma-bench -- slo --output-dir /tmp/fantasma-bench-append-loop-r{1,2} --scenario live-append-small-blobs` both preserved the expected `225` checkpoint samples and landed at `checkpoint derived p50/p95 217/322ms` with final derived readiness `9ms` and `7ms`. That missed the `<= 292ms` keep bar against the kept March 15 append baseline `319/325ms`, so the code changes were backed out and the repository returned to the prior baseline. The traces showed why the hypothesis failed: `build_rollups` rose to roughly `25.2%` to `25.7%` share while `upsert_dim4_buckets` and `upsert_dim3_buckets` still consumed roughly `23.7%` to `24.7%` and `18.8%`, with `resolve_dim4_members` adding about `8.7%`. Result: reverted after measurement; keep the diagnosis, not the implementation.
- Completed the Swift SDK hardening slice on March 15, 2026. The iOS client now fail-fast validates explicit event properties against the published ingest contract before persistence, treats malformed `202 Accepted` envelopes as invalid responses that preserve queued rows, and makes destination-drift reconfiguration destructive to still-queued rows while keeping `clear()` identity-only, including after app relaunches because the durable queue now remembers which destination owns its rows. The JSON event schema now matches OpenAPI's reserved property-name list, and the SDK/architecture docs now describe the same validation, durability, and reconfiguration semantics.
- Completed the benchmark usability reset on March 15, 2026. `fantasma-bench slo` now has explicit `iterative` and `publication` modes, defaults to the fast directional iterative gate, keeps publication mode as the only product-facing artifact path, and prints a compact pinned-baseline verdict block with representative append, mixed repair, checkpoint-derived vs final-derived readiness comparisons, dominant top-level Stage B phase change, and overall better/flat/worse. Root cause for the unusable prior default path was harness policy, not worker throughput: the representative family was posting `180_000` append blobs and then forcing about `9_000` to `12_000` checkpoint waits per live scenario, so iteration cost was dominated by upload-count and readiness-poll fan-out rather than by a single worker phase. The new iterative policy keeps the 30-day representative identity but cuts the live cohort to `250` installs/day and checkpoint density to every `200th` append blob, every `25th` offline-flush blob, and every `10th` repair blob; the pinned iterative baseline now lives under `crates/fantasma-bench/baselines/slo-iterative-default.json`, ignores checkpoint p95 comparisons when the current sample count no longer matches the pinned baseline, and reports dominant Stage B phase as `unknown` instead of `changed` when the current sidecar is missing.
  Verification: `cargo test -p fantasma-bench --quiet`, `cargo test -p fantasma-bench publication_mode_default_selection_keeps_the_full_live_worker_family`, `cargo test -p fantasma-bench iterative_live_append_small_blob_schedule_uses_reduced_cohort_and_sparse_checkpoints`, `cargo test -p fantasma-bench metric_verdict_uses_the_larger_of_one_second_or_five_percent_threshold`, `cargo test -p fantasma-bench stage_b_phase_verdict_reports_changed_when_the_dominant_phase_differs`, `docker compose -f infra/docker/compose.bench.yaml config`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check` all passed. Fresh smoke evidence in this slice: `live-append-small-blobs` iterative completed in `184.42s` with `225` checkpoint samples, `checkpoint derived p95 327ms`, and `derived_metrics_ready_ms 8`; `live-append-plus-light-repair` iterative completed in `108.01s` with `234` checkpoint samples, `checkpoint derived p95 429ms`, `derived_metrics_ready_ms 7`, and Stage B dominant phase `shared_bucket_finalize`; `stress-append-30d` publication completed in `58.63s` with `derived_metrics_ready_ms 22_832`. A full no-filter iterative default run also completed in `306.49s` and ended with the intended compact verdict block: representative append `flat`, mixed repair `flat`, dominant Stage B phase `unchanged`, overall `flat`. Conclusion: the default SLO path is now fast enough to use during normal worker-performance iteration.
- Completed the realistic benchmark reset on March 15, 2026. `fantasma-bench` now treats `representative` plus `mixed-repair` as the default product-facing worker gate, keeps the old large append/backfill/repair windows reclassified under `stress-*`, and publishes grouped read latency through a separate `reads-visibility` suite instead of mixing it into worker-freshness tuning. The representative family now models live upload traffic as deterministic small-blob schedules with checkpoint freshness sampling, keeps the agreed `1,000` active installs/day baseline with `30` events/install/day, and locks that scale with a regression test, while Stage B attribution sidecars stay available for representative, mixed-repair, and stress runs. Product-facing publication now includes `representative` plus `mixed-repair` together; `stress` and `reads-visibility` may publish separately for regression visibility.
  Verification: `cargo test -p fantasma-bench --quiet`, `cargo test -p fantasma-bench live_append_small_blob_schedule_samples_every_20th_append_blob -- --nocapture`, `cargo test -p fantasma-bench offline_flush_and_repair_live_schedules_keep_special_blobs_checkpointed -- --nocapture`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `docker compose -f infra/docker/compose.bench.yaml config`, and `git diff --check` all passed. Follow-up runtime smoke on March 15, 2026 also completed `cargo run -p fantasma-bench -- slo --output-dir /tmp/fantasma-bench-smoke-representative --scenario live-append-small-blobs` and `cargo run -p fantasma-bench -- slo --output-dir /tmp/fantasma-bench-smoke-reads --scenario reads-visibility-30d`, both with published suite artifacts and `Budget: PASS`. Earlier interrupted smoke attempts remain in the history, but the benchmark reset no longer lacks a completed end-to-end runtime artifact. Mixed-repair and stress still do not have fresh completed smoke artifacts in this follow-up.
- Completed the install-day Stage B finalization redesign on March 15, 2026. Added internal migration `0014_session_daily_installs_rebuild_queue.sql` for a durable `(project_id, day, install_id)` queue, kept direct store callers on the existing full-day rebuild helper, and taught Stage B finalization to claim queued install-day rows into a transaction-local scope table and rewrite only those `session_daily_installs` rows before the existing `session_daily` aggregate rebuild. Repair jobs now enqueue install-day rebuild rows idempotently alongside the existing shared bucket queue, the normal Stage B path now raises an invariant violation if queue rows are missing or only partially cover the requested days, and Project Sweep plus the additive nested `finalization_subphases` / `session_daily_subphases` sidecars stayed intact. Focused/new coverage that now passes: `cargo test -p fantasma-store rebuild_session_daily_days_helper_uses_full_rebuild_for_direct_callers_and_queue_scope_for_stage_b`, `./scripts/docker-test.sh -p fantasma-store queued_session_daily_install_rebuild -- --nocapture`, `./scripts/docker-test.sh -p fantasma-store queued_session_daily_install_rebuild_requires -- --nocapture`, `./scripts/docker-test.sh -p fantasma-store later_same_day_refill_waits_until_current_sweep_commits_and_keeps_matching_queue_rows -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker repeated_same_install_day_repairs_collapse_to_one_pending_membership_rewrite -- --nocapture`, and `./scripts/docker-test.sh -p fantasma-worker pending_membership_rebuild -- --nocapture`. Required existing verification that also passed in this slice: `./scripts/docker-test.sh -p fantasma-store --lib -- --quiet`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_keeps_grouped_session_app_version_fixed_after_late_event_repair -- --nocapture`, `cargo test -p fantasma-bench summarize_stage_b_trace_records_reports_finalization_subphases_without_changing_top_level_accounting`, `cargo test -p fantasma-bench summarize_stage_b_trace_records_reports_session_daily_subphases_without_changing_finalization_shape`, `cargo test -p fantasma-bench --quiet`, `cargo test -p fantasma-worker scheduler::tests::worker_config_defaults_match_repo_defaults -- --exact`, `docker compose -f infra/docker/compose.bench.yaml config`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`. Fresh 30d readiness run in this slice: `cargo run -p fantasma-bench -- slo --output-dir /tmp/split-worker-stage-b-investigation --repetitions-30d 1 --scenario append-30d --scenario backfill-30d --scenario repair-30d`, which completed with `append-30d 22_789ms`, `backfill-30d 38_035ms`, and `repair-30d 38_852ms`. The nested bottleneck moved materially: `session_daily_installs_write` dropped to `6_387ms` and `48.36%` of `session_daily_subphases` in `backfill-30d` (no longer dominant there; `session_daily_aggregate_write` is `6_812ms`) and to `3_891ms` / `57.69%` in `repair-30d` (still dominant inside `session_daily`, but much smaller in absolute cost). `finalization_session_daily_rebuild` is no longer the dominant `finalization_subphase` in either rebuild-style scenario; `finalization_metric_dim1_rebuild` now leads both (`18_960ms` in `backfill-30d`, `12_846ms` in `repair-30d`). Outcome: keep. This redesign beat the trusted baseline comfortably on both rebuild-style scenarios and moved the structural hot path up from `session_daily_installs_write` to the shared metric bucket rebuilds, with `session_daily_aggregate_write` now slightly ahead of install maintenance in `backfill-30d`. Reviewer follow-up on the same date added the dedicated queue interleaving regression requested before closure: `later_same_day_refill_waits_until_current_sweep_commits_and_keeps_matching_queue_rows` proves that a later same-day refill cannot enqueue the matching day bucket until the current sweep commits, and that the refill leaves aligned day-bucket and install-day queue rows for the next pass instead of being silently consumed by the in-flight finalization transaction.
- Completed the session-daily attribution-reset slice on March 14, 2026 without changing finalization semantics. The failed `pg_temp.session_daily_rebuild_source` rewrite has been selectively backed out from `rebuild_session_daily_days_in_tx()` so the helper/query path matches the attribution-reset baseline shape again, including the dedicated `rebuild_session_daily_installs_days_in_tx()` helper and the aggregate `install_rollups` CTE. Added one more additive sidecar layer, `session_daily_subphases`, under `finalization_session_daily_rebuild`: `rebuild_finalize` trace records now carry `session_daily_subphases_ms`, `stage-b.json` now summarizes `session_daily_subphases` and `dominant_session_daily_subphase`, and the existing top-level Stage B plus `finalization_subphases` outputs remain unchanged in shape. In the restored baseline there is no standalone `session_daily_source_build` boundary, so that time remains folded into `session_daily_installs_write` / `session_daily_aggregate_write`; `session_daily_cleanup` is emitted only when the delete path measures above zero. Fresh verification in this pass: `cargo test -p fantasma-store rebuild_session_daily_days_helper_matches_attribution_reset_shape -- --nocapture`, `cargo test -p fantasma-worker stage_b_trace_writer_emits_finalization_subphases_for_rebuild_finalize_records -- --nocapture`, `cargo test -p fantasma-bench summarize_stage_b_trace_records_reports_finalization_subphases_without_changing_top_level_accounting -- --nocapture`, `cargo test -p fantasma-bench summarize_stage_b_trace_records_reports_session_daily_subphases_without_changing_finalization_shape -- --nocapture`, `cargo test -p fantasma-bench summarize_stage_b_trace_records_does_not_fabricate_session_daily_subphases_when_absent -- --nocapture`, `./scripts/docker-test.sh -p fantasma-store rebuild_session_daily_days_in_tx_ -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker rebuild_finalization_sweeps_visible_project_queue_in_one_pass -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_keeps_grouped_session_app_version_fixed_after_late_event_repair -- --nocapture`, `cargo test -p fantasma-bench --quiet`, `cargo test -p fantasma-worker scheduler::tests::worker_config_defaults_match_repo_defaults -- --exact`, and `docker compose -f infra/docker/compose.bench.yaml config`. Fresh disposable rerun in this pass: `cargo run -p fantasma-bench -- slo --output-dir /tmp/split-worker-stage-b-investigation --repetitions-30d 1 --scenario append-30d --scenario backfill-30d --scenario repair-30d`, which completed with `append-30d 22_731ms`, `backfill-30d 111_112ms`, and `repair-30d 105_433ms`. Both rebuild-style scenarios still emitted `stage-b.json` / `stage-b-trace.jsonl` and now include `session_daily_subphases`; `append-30d` again emitted no Stage B sidecar, which remains acceptable for the non-rebuild sanity scenario. Trust result: partial only. `repair-30d` stayed within the attribution-reset trust range, but `backfill-30d` landed `1_507ms` above its trusted upper bound, so the restored behavior is not a perfect replay of the attribution-reset baseline. Even with that miss, both rebuild-style scenarios still converge on the same dominant nested `session_daily` cost: `session_daily_installs_write` at `89.87%` for `backfill-30d` and `85.80%` for `repair-30d`, well above the dominance bar and ahead of `session_daily_aggregate_write`. Conclusion: the deeper attribution is strong enough to justify the next optimization slice, but that slice should stay narrowly on the repeated `session_daily_installs` rebuild/write path rather than broadening scope.
- Implemented the session-daily-only finalization optimization on March 14, 2026 and kept the change local to `rebuild_session_daily_days_in_tx()`. The helper now builds one transaction-local ephemeral touched-session source (`pg_temp.session_daily_rebuild_source`) per project/finalization pass, rebuilds both `session_daily_installs` and `session_daily` from that shared source, and computes `session_daily.active_installs` directly from the same touched rows instead of rescanning `sessions` and then re-reading `session_daily_installs`. Extended the helper-local regression coverage so the structure and correctness stay explicit: `rebuild_session_daily_days_helper_uses_one_shared_ephemeral_source`, `rebuild_session_daily_days_in_tx_leaves_untouched_days_alone`, `rebuild_session_daily_days_in_tx_rollback_preserves_existing_rows`, `./scripts/docker-test.sh -p fantasma-worker rebuild_finalization_sweeps_visible_project_queue_in_one_pass -- --nocapture`, and `./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_keeps_grouped_session_app_version_fixed_after_late_event_repair -- --nocapture` all passed in this slice, along with the required preflight commands. Fresh disposable rerun in this pass: `cargo run -p fantasma-bench -- slo --output-dir /tmp/split-worker-stage-b-investigation --repetitions-30d 1 --scenario append-30d --scenario backfill-30d --scenario repair-30d`, which completed with `append-30d 23_011ms`, `backfill-30d 106_920ms`, and `repair-30d 106_781ms`. Both rebuild-style scenarios still emitted `stage-b.json` / `stage-b-trace.jsonl` with nested `finalization_subphases`, and both still show `shared_bucket_finalize` as the dominant active Stage B phase. The optimization did not clear its handoff bar: `finalization_session_daily_rebuild` remains the dominant nested finalization sub-phase at `71.24%` for `backfill-30d` and `72.93%` for `repair-30d`, and both rebuild-style readiness numbers regressed versus the attribution-reset baseline (`99_641ms` / `104_334ms`). Conclusion: keep the next slice strictly on the `session_daily` finalization path; this shared-source rewrite did not move enough active time out of `finalization_session_daily_rebuild`.
- Completed the finalization attribution-reset slice on March 14, 2026 without changing finalization semantics. The failed SQL-direct `rebuild_session_metric_buckets_in_tx()` optimization has been backed out in favor of the pre-failure metric rebuild path, while Project Sweep and the corrected Stage B sidecar plumbing remain intact. `rebuild_finalize` trace records now carry nested finalization sub-phase timing around the existing helper boundaries (`finalization_drain_setup`, `finalization_session_daily_rebuild`, `finalization_metric_total_rebuild`, `finalization_metric_dim1_rebuild`, `finalization_metric_dim2_rebuild`, plus `finalization_scope_delete`), and `stage-b.json` now summarizes those sub-phases and reports a `dominant_finalization_subphase` without changing the existing top-level `shared_bucket_finalize` accounting. Backed out the failed-rewrite-only worker regression and helper baggage so the new measurement pass stayed anchored on Project Sweep plus corrected attribution rather than the failed local hypothesis. Fresh verification in this pass: `cargo test -p fantasma-worker stage_b_trace_writer_emits_jsonl_when_trace_path_is_set -- --nocapture`, `cargo test -p fantasma-worker stage_b_trace_writer_emits_finalization_subphases_for_rebuild_finalize_records -- --nocapture`, `cargo test -p fantasma-bench summarize_stage_b_trace_records_reports_finalization_subphases_without_changing_top_level_accounting -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker rebuild_finalization_sweeps_visible_project_queue_in_one_pass -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_keeps_grouped_session_app_version_fixed_after_late_event_repair -- --nocapture`, `cargo test -p fantasma-bench --quiet`, `cargo test -p fantasma-worker scheduler::tests::worker_config_defaults_match_repo_defaults -- --exact`, and `docker compose -f infra/docker/compose.bench.yaml config`. Fresh disposable rerun in this pass: `cargo run -p fantasma-bench -- slo --output-dir /tmp/split-worker-stage-b-investigation --repetitions-30d 1 --scenario append-30d --scenario backfill-30d --scenario repair-30d`, which completed with `append-30d 21_635ms`, `backfill-30d 99_641ms`, and `repair-30d 104_334ms`. `append-30d` again emitted no Stage B sidecar, which remains acceptable for the non-rebuild sanity scenario. Both rebuild-style scenarios produced `stage-b.json` / `stage-b-trace.jsonl`, and both still show `shared_bucket_finalize` as the dominant active Stage B phase. The nested finalization breakdown is now strong enough to justify the next optimization slice: `finalization_session_daily_rebuild` dominates `shared_bucket_finalize` in both scenarios at `69.01%` for `backfill-30d` and `74.24%` for `repair-30d`, well above the dominance bar and ahead of the next-largest `finalization_metric_dim1_rebuild` sub-phase. Conclusion: the next change should remain finalization-local and target the repeated `session_daily` rebuild work first.
- Attempted the next shared-finalization optimization on March 14, 2026 and kept the change local to the metric rebuild path inside Stage B finalization. `rebuild_session_metric_buckets_in_tx()` no longer loads sessions and install-first-seen rows into Rust to assemble total/dim1/dim2 metric deltas; it now deletes the requested bucket scope and rebuilds the total, dim1, and dim2 metric tables directly in SQL for the requested bucket set. Added a focused worker regression, `rebuild_session_metric_buckets_replaces_requested_scope_and_preserves_untouched_rows`, that seeds real session/install data plus stale metric rows and proves the requested rebuild scope is fully replaced while untouched stale buckets remain intact. Fresh verification in this pass: `cargo fmt --all`, `cargo test -p fantasma-worker --no-run`, `./scripts/docker-test.sh -p fantasma-worker rebuild_session_metric_buckets_replaces_requested_scope_and_preserves_untouched_rows -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker rebuild_finalization_sweeps_visible_project_queue_in_one_pass -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker --lib -- --quiet`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_keeps_grouped_session_app_version_fixed_after_late_event_repair -- --nocapture`, `cargo test -p fantasma-bench --quiet`, `cargo test -p fantasma-worker scheduler::tests::worker_config_defaults_match_repo_defaults -- --exact`, and `docker compose -f infra/docker/compose.bench.yaml config`. Fresh screen in this pass: `cargo run -p fantasma-bench -- slo --output-dir /tmp/split-worker-stage-b-investigation --repetitions-30d 1 --scenario append-30d --scenario backfill-30d --scenario repair-30d`, which completed with the disposable `/tmp` artifact and published `append-30d 24_266ms`, `backfill-30d 104_215ms`, and `repair-30d 105_266ms`. Both rebuild-style scenarios still produced `stage-b.json` / `stage-b-trace.jsonl`; `append-30d` again emitted no Stage B sidecar, which remains acceptable for the non-rebuild sanity scenario. Result: the strict handoff bar was not met. `shared_bucket_finalize` remained the dominant active Stage B phase in both rebuild-style scenarios at `95.87%` of active measured Stage B time for `backfill-30d` and `97.41%` for `repair-30d`, and both rebuild-style readiness numbers regressed versus the post-Project-Sweep reference (`backfill-30d 96_371ms`, `repair-30d 103_110ms`). The slowest finalize records are still one-project sweeps over modest scopes (`58` claimed rebuild rows / `29` day buckets / `29` hour buckets in `backfill-30d`, `64` / `22` / `42` in `repair-30d`), with `shared_bucket_finalize` distribution still concentrated around `p50 115ms` / `p95 341ms` for backfill and `p50 93ms` / `p95 411ms` for repair. Conclusion: keep the next slice finalization-local; the remaining sub-bottleneck still appears to be the repeated per-project metric bucket rebuild statements inside `rebuild_session_metric_buckets_in_tx()` / adjacent rebuild helpers rather than queueing or broader Stage B flow.
- Attempted the Project Sweep shared-finalization optimization on March 14, 2026 and kept the change local to Stage B finalization. `session_rebuild_queue` draining now sweeps all currently pending rebuild buckets visible in the transaction for projects touched by the seed claim, unions them with the claimed rows, and rebuilds once per project over that swept scope. Fresh verification in this pass: `cargo test -p fantasma-bench --quiet`, `cargo test -p fantasma-worker stage_b_trace_writer -- --nocapture`, `cargo test -p fantasma-worker scheduler::tests::worker_config_defaults_match_repo_defaults -- --exact`, `docker compose -f infra/docker/compose.bench.yaml config`, `./scripts/docker-test.sh -p fantasma-store claim_and_sweep_pending_session_rebuild_buckets_sweeps_visible_project_scope -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker rebuild_finalization_sweeps_visible_project_queue_in_one_pass -- --nocapture`, `./scripts/docker-test.sh -p fantasma-store --lib -- --quiet`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`. Fresh screen in this pass: `cargo run -p fantasma-bench -- slo --output-dir /tmp/split-worker-stage-b-investigation --repetitions-30d 1 --scenario append-30d --scenario backfill-30d --scenario repair-30d`, which exited non-zero after publishing the disposable `/tmp` artifact with `append-30d 20_949ms`, `backfill-30d 96_371ms`, and `repair-30d 103_110ms`. Both rebuild-style scenarios improved versus the corrected pre-sweep reference (`backfill-30d 105_547ms`, `repair-30d 141_370ms`), and both still produced `stage-b.json` / `stage-b-trace.jsonl`; `append-30d` again emitted no Stage B sidecar, which remains acceptable for the non-rebuild sanity scenario. Result: the strict handoff bar was not met. `shared_bucket_finalize` remained the dominant active Stage B phase in both rebuild-style scenarios at `96.31%` of active measured Stage B time for `backfill-30d` and `97.56%` for `repair-30d`. Conclusion: keep the next slice on shared bucket finalization and do not move on to `90d`, publication, tuple tuning, or broader Stage B work yet.
- Completed the corrected non-publishable Stage B attribution slice on March 14, 2026 in `/tmp/split-worker-stage-b-investigation` at the checked-in defaults (`session_batch_size=1000`, `event_batch_size=5000`, `session_incremental_concurrency=8`, `session_repair_concurrency=2`). Fresh verification in this corrected pass: `cargo test -p fantasma-bench summarize_stage_b_trace_records_separates_claim_lag_backlog_from_active_work -- --nocapture`, `cargo test -p fantasma-bench --quiet`, `cargo test -p fantasma-worker stage_b_trace_writer -- --nocapture`, `cargo test -p fantasma-worker scheduler::tests::worker_config_defaults_match_repo_defaults -- --exact`, `docker compose -f infra/docker/compose.bench.yaml config`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`. Fresh corrected screen in this pass: `cargo run -p fantasma-bench -- slo --output-dir /tmp/split-worker-stage-b-investigation --repetitions-30d 1 --scenario append-30d --scenario backfill-30d --scenario repair-30d`, which exited non-zero after publishing the disposable `/tmp` artifact with `append-30d 22_372ms`, `backfill-30d 105_547ms`, and `repair-30d 141_370ms`. `append-30d` again emitted no Stage B trace sidecar, which is acceptable for the non-rebuild sanity scenario. `backfill-30d` remained `51_644ms` slower than the March 13 baseline `53_903ms` and `7_670ms` faster than the failed `5000 / 5000 / 8 / 2` split candidate `113_217ms`; `repair-30d` remained `139_664ms` slower than the March 13 baseline `1_706ms` and `40_219ms` worse than the failed candidate `101_151ms`. The trace plumbing now reports `row_mutation_lag_before_claim` only as backlog context instead of counting it in `measured_stage_b_ms`, so the sidecars keep backlog visible without letting overlapping queue delay dominate the active-work attribution. Corrected attribution conclusion: `shared_bucket_finalize` now dominates both rebuild-style scenarios by the plan rule, at `95.76%` of active measured Stage B time for `backfill-30d` and `95.35%` for `repair-30d`, while the next-largest active phase in either case stays below `2.43%`. The backlog context is still large (`backfill-30d` row-mutation lag before claim `p50 4_949ms`, `p95 90_501ms`, `max 104_778ms`; `repair-30d` `p50 28_403ms`, `p95 130_984ms`, `max 140_935ms`), but it is no longer used to pick the next architectural target. Conclusion: stop broad Stage B investigation here, do not tune tuples, and make the next fix slice target only shared bucket drain / rebuild finalization on the split worker path.
- Completed the non-publishable split-worker defaults screen on March 14, 2026 in `/tmp/split-worker-defaults-30d-screen` with `append-30d`, `backfill-30d`, and `repair-30d` at the checked-in defaults (`session_batch_size=1000`, `event_batch_size=5000`, `session_incremental_concurrency=8`, `session_repair_concurrency=2`). Fresh preflight in this pass: `cargo test -p fantasma-bench --quiet`, `cargo test -p fantasma-worker scheduler::tests::worker_config_defaults_match_repo_defaults -- --exact`, and `docker compose -f infra/docker/compose.bench.yaml config`. Fresh screening evidence in this pass: `cargo run -p fantasma-bench -- slo --output-dir /tmp/split-worker-defaults-30d-screen --repetitions-30d 1 --scenario append-30d --scenario backfill-30d --scenario repair-30d` exited non-zero after publishing the disposable `/tmp` artifact with `append-30d 21_524ms`, `backfill-30d 101_705ms`, and `repair-30d 109_742ms`. Interpretation: this screen did not recover the split. `backfill-30d` still failed the `60_000ms` bar and remained `47_802ms` (`88.7%`) slower than the March 13 baseline `53_903ms`, although it improved `11_512ms` (`10.2%`) versus the failed `5000 / 5000 / 8 / 2` candidate `113_217ms`. `repair-30d` failed the `2_706ms` guardrail by a wide margin, regressed `108_036ms` (`6332.7%`) versus the March 13 baseline `1_706ms`, and was `8_591ms` (`8.5%`) worse than the failed candidate `101_151ms`. Conclusion: stop the benchmark escalation here, do not run `90d`, do not create a repo artifact under `artifacts/performance/`, and treat the split worker as still showing a Stage B regression rather than a candidate-tuple-only issue.
- Completed the split-worker candidate rerun on March 14, 2026 and published the full 12-scenario artifact under `artifacts/performance/2026-03-14-session-apply-repair-split-promote-candidate/` against the pinned baseline `artifacts/performance/2026-03-13-dim4-impact-full-r2/summary.json`. Fresh preflight in this pass: `cargo test -p fantasma-bench --quiet`, `docker compose -f infra/docker/compose.bench.yaml config`, Docker image warm-up for `rust:1.92-bookworm` and `debian:bookworm-slim`, and a disposable `append-30d` smoke run with the candidate tuple. Fresh full-run evidence in this pass: `cargo run -p fantasma-bench -- slo --output-dir artifacts/performance/2026-03-14-session-apply-repair-split-promote-candidate --worker-session-batch-size 5000 --worker-event-batch-size 5000 --worker-session-incremental-concurrency 8 --worker-session-repair-concurrency 2` completed with a full 12-scenario artifact and exited non-zero because `backfill-30d` and `repair-30d` exceeded the hard `30d` freshness budgets, plus `jq -r '.scenarios | length' artifacts/performance/2026-03-14-session-apply-repair-split-promote-candidate/summary.json` confirmed all `12` scenarios published. Result: the artifact is publishable as a failed candidate, but it does not earn default promotion. `append-30d` and `append-90d` improved to `9_150ms` and `23_716ms`, yet `backfill-30d` regressed to `113_217ms`, `backfill-90d` regressed to `327_066ms`, and repair regressed far beyond the March 14 guardrail (`repair-30d 101_151ms`, `repair-90d 568_298ms`, `repair-180d 1_331_810ms`). Grouped reads stayed green, so the checked-in defaults remain `1000 / 5000 / 8 / 2`, and the next diagnostic step is a same-code default rebaseline rather than a promotion.
- Fixed the sixth March 14, 2026 session-repair review follow-up in the split worker diff. The repair-batch join-error path now carries per-task claim context, so a repair worker panic or external cancellation after the short claim commit no longer strands `claimed_at`. Each spawned repair task records its claimed frontier under its Tokio task ID until it resolves, `process_session_repair_batch_inner()` now releases any unresolved claim before surfacing a join error, and unresolved join-error cleanup contexts stay retained until claim release succeeds so a transient cleanup failure can retry instead of discarding the last handle to the claimed row. Added worker regressions that force a post-claim panic and a post-claim panic with one forced join-error cleanup failure, and both verify the repair frontier remains queued and reclaimable afterward. Fresh verification in this follow-up: `./scripts/docker-test.sh -p fantasma-worker panicked_repair_worker_releases_claim_for_retry -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker join_error_claim_cleanup_retries_before_dropping_task_context -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker --lib -- --quiet`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_keeps_grouped_session_app_version_fixed_after_late_event_repair -- --nocapture`, `cargo fmt --all`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`.
- Fixed the fifth March 14, 2026 session-repair review follow-up in the split worker diff. Repair batches no longer cancel already-claimed peer jobs when one install fails. `process_session_repair_batch_inner()` now records the first error but keeps joining the other claimed workers so each one can either commit successfully or run its own claim-release cleanup before the batch drains `session_rebuild_queue` and returns the error. Added a worker regression that blocks one claimed repair, waits for a peer failure to release its own claim, verifies the batch still has not returned, then resumes the blocked repair and confirms its frontier is cleared instead of being stranded. Fresh verification in this follow-up: `./scripts/docker-test.sh -p fantasma-worker repair_batch_waits_for_claimed_jobs_to_finish_before_returning_error -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker --lib -- --quiet`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_keeps_grouped_session_app_version_fixed_after_late_event_repair -- --nocapture`, `cargo fmt --all`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`.
- Fixed the fourth March 14, 2026 session-repair review follow-up in the split worker diff. Mixed-success repair batches no longer drop queued shared-bucket finalization when a later install fails: `process_session_repair_batch_inner()` now records the first batch error, waits for the batch to quiesce, drains `session_rebuild_queue`, and only then returns the error. Added a worker regression that forces one repair to queue rebuild work while a second blocked install fails, and verifies the successful repair's project day aggregate is still finalized before the batch surfaces the error. Fresh verification in this follow-up: `./scripts/docker-test.sh -p fantasma-worker mixed_repair_batch_still_drains_shared_bucket_finalization_before_returning_error -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker --lib -- --quiet`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_keeps_grouped_session_app_version_fixed_after_late_event_repair -- --nocapture`, `cargo fmt --all`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`.
- Fixed the third March 14, 2026 session-repair review follow-up in the split worker diff. A claimed `session_repair_jobs` row no longer strands indefinitely if replay fails after the short claim transaction commits: `process_next_session_repair_job()` now opens a fresh cleanup transaction on any post-claim Stage B failure and clears only `claimed_at`, preserving the current repair frontier for retry instead of deleting or orphaning it. Added a worker regression that forces a post-claim failure with no `install_session_state` and a store regression that verifies explicit claim release keeps the frontier claimable. Fresh verification in this follow-up: `./scripts/docker-test.sh -p fantasma-worker repair_job_failure_releases_claim_for_retry -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker --lib -- --quiet`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_keeps_grouped_session_app_version_fixed_after_late_event_repair -- --nocapture`, `cargo fmt --all`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`.
- Fixed the second March 14, 2026 session-repair review pass in the split worker diff. `session_repair` now claims `session_repair_jobs` in a short transaction and releases those row locks before the full replay transaction, so `session_apply` no longer waits on claimed repair rows while holding the raw `"sessions"` offset lock. Shared project hour/day session bucket rebuilds now queue through `session_rebuild_queue` and are drained after repair-batch processing instead of being rebuilt inline per install, which restores safe serialization for overlapping project buckets under concurrent repairs. Stage A repair-frontier widening is now batched inside the coordinator transaction, and worker regressions now cover both deferred shared-bucket finalization and the raw-offset lock decoupling. Fresh verification in this follow-up: `./scripts/docker-test.sh -p fantasma-worker repair_job_defers_project_bucket_rebuild_until_batch_finalization -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker apply_lane_does_not_hold_raw_offset_lock_while_waiting_on_claimed_repair -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker --lib -- --quiet`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_keeps_grouped_session_app_version_fixed_after_late_event_repair -- --nocapture`, `cargo fmt --all`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`.
- Fixed the March 14, 2026 session-repair review regressions in the current worker split diff. `repair_install_batch()` now derives recompute bounds from the true pending-event timestamp extrema rather than raw-id frontier order, which restores late-event repair correctness for out-of-order batches, and the public `fantasma_worker::process_session_batch()` helper now drains repair jobs after Stage A so in-process callers once again see final session metrics after a single call while the scheduler keeps the split apply/repair lane behavior through `process_session_batch_with_config()`. Added a worker regression covering out-of-order pending timestamps and updated the lower-level worker tests that intentionally exercise Stage A enqueueing to call the split-lane helper directly. Fresh verification in this fix: `./scripts/docker-test.sh -p fantasma-worker repair_window_uses_timestamp_extrema_for_out_of_order_pending_events -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline pipeline_keeps_grouped_session_app_version_fixed_after_late_event_repair -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker --lib -- --quiet`, `cargo fmt --all`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets -- -D warnings`.
- Completed the session apply/repair worker split on March 14, 2026. The worker now runs independent `session_apply`, `session_repair`, and `event_metrics` lanes; `session_apply` remains the only owner of the raw `"sessions"` offset; repair work is routed through install-scoped durable `session_repair_jobs` frontier state; `install_session_state.last_processed_event_id` remains the only authoritative applied replay frontier; shared project hour/day bucket finalization now flows through `session_rebuild_queue` after repair commits; and later repair frontiers can survive an earlier in-flight claim and replay safely. Docs were updated in `docs/architecture.md`, `docs/deployment.md`, and `docs/performance.md` to match the split. Fresh verification in this slice: `cargo test -p fantasma-worker --lib --no-run`, `./scripts/docker-test.sh -p fantasma-store session_repair_jobs -- --nocapture`, `./scripts/docker-test.sh -p fantasma-worker --lib -- --quiet`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets -- -D warnings`.
- Repaired the March 19, 2026 deletion-fence follow-up after review. Project-processing leases are now committed outside the guarded mutation transaction so deletion readiness can observe real in-flight work; deletion scheduling no longer head-of-line blocks unrelated queued jobs behind one lease-blocked project because only runnable deletion jobs are claimed; stale older-epoch leases now age out after `1h` of missed heartbeats instead of blocking deletion forever; worker lanes refresh live lease heartbeats at their long-running checkpoints; and session-repair join-error cleanup still tracks and retries both the repair claim row and the external project lease so panics do not strand a deletion fence indefinitely. Added regressions covering the real external lease helpers, panicked/join-error repair cleanup, deletion waiting on held leases, deletion waiting while the writer lifecycle keeps a separate transaction open, cross-project scheduler fairness, and stale-lease expiry. Fresh verification in this repair: `cargo fmt --all`, `cargo test -p fantasma-store --lib --no-run`, `cargo test -p fantasma-worker --lib --no-run`, `cargo test -p fantasma-worker --test pipeline --no-run`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-store project_processing_lease_detects_fence_epoch_advance -- --quiet`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker panicked_repair_worker_releases_claim_for_retry -- --quiet`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker join_error_claim_cleanup_retries_before_dropping_task_context -- --quiet`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline project_deletion_waits_for_older_processing_leases -- --quiet`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline project_deletion_waits_while_open_writer_transaction_holds_visible_lease -- --quiet`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline blocked_deletion_does_not_head_of_line_block_other_projects -- --quiet`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline stale_processing_leases_do_not_block_deletion_forever -- --quiet`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline range_delete_worker_rebuilds_state_and_reactivates_project -- --exact --quiet`, `FANTASMA_DOCKER_TEST_KEEP_CACHE=1 ./scripts/docker-test.sh -p fantasma-worker --test pipeline project_purge_worker_removes_project_and_keeps_history -- --exact --quiet`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets -- -D warnings`.
- Completed the post-dim4 session-lane freshness harness pass on March 14, 2026. `fantasma-bench` SLO runs now accept explicit worker overrides plus `--repetitions-30d`, publish an additive `run_config` block in per-scenario and top-level summary artifacts while keeping `host.json` host-only, and render a temporary benchmark Compose file so screening runs use the same stack path as published runs. The tuning sweep identified stronger append/backfill settings, but none of the screened tuples satisfied the slice repair guardrail, so the checked-in worker defaults remain `session_batch_size=1000`, `event_batch_size=5000`, `session_incremental_concurrency=8`, and `session_repair_concurrency=2` in both `WorkerConfig` and the checked-in Compose files. Fresh verification in this correction pass: `cargo test -p fantasma-bench --quiet`, `cargo test -p fantasma-worker scheduler::tests::worker_config_defaults_match_repo_defaults -- --exact`, `docker compose -f infra/docker/compose.yaml config`, and `docker compose -f infra/docker/compose.bench.yaml config`. A full corrected SLO rerun was started under `artifacts/performance/2026-03-14-session-freshness-promoted-defaults-r2/`, but it stopped at `reads-90d` when Docker tried to resolve `rust:1.92-bookworm` and `debian:bookworm-slim` from `registry-1.docker.io` without network access; the partial artifact still confirmed that `5000 / 5000 / 8 / 2` would not satisfy the repair acceptance bar (`repair-30d 1_706 -> 2_344`, `repair-90d 5_192 -> 9_678`).
- Repaired the March 13, 2026 dim2/dim3 bucketed read-index CI regression after the `Expand event metrics to dim4` and `Publish dim4 benchmark artifacts` pushes. Root cause: migration `0012_event_metric_dim4_cleanup.sql` added the new dim4 key-aware plus value-specific read indexes, but it did not carry the same bounded later-dimension read pattern forward for the existing dim2/dim3 bucket tables. The store regression tests now assert the dim2/dim3 value-specific EXPLAIN plans and also assert that `0012` itself contains the dim2/dim3 bucket-index realignment, so unrelated earlier migration edits cannot mask a missing `0012` repair. Prevention note: whenever bucket arity or read-index shape changes, update the lower-arity bucket migrations, migration-text assertions, and EXPLAIN-backed read-path tests in the same change. Fresh verification in this follow-up: `cargo test -p fantasma-store tests::forward_migration_adds_dim4_bucket_storage_and_cleans_legacy_daily_tables -- --exact`, `./scripts/docker-test.sh -p fantasma-store event_metrics_dim -- --nocapture`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets -- -D warnings`.
- Repaired the March 13, 2026 CLI CI regression after the `Add Fantasma operator CLI` push. Root cause: the new `fantasma-cli` metrics helper tripped `clippy::too_many_arguments` under the repository-wide `cargo clippy --workspace --all-targets -- -D warnings` job even though the narrower package checks were green locally. The CLI now passes a small internal `MetricsRequest` struct instead of a 9-argument helper signature, and `AGENTS.md` now explicitly requires workspace-affecting Rust changes to run the same workspace-level `cargo fmt --all --check` and `cargo clippy --workspace --all-targets -- -D warnings` checks that CI runs before push. Fresh verification in this repair: `gh run view 23071356317 --log-failed`, `cargo fmt --all`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets -- -D warnings`. `cargo test --workspace --quiet` still needs the CI/Docker Postgres setup and fails locally without `DATABASE_URL`, consistent with the existing repository test model.
- Hardened the operator CLI test surface on March 13, 2026. The CLI crate now has explicit integration coverage for `auth logout` with explicit and active-instance resolution, `projects use` persistence, ingest-key creation without local persistence, `keys list --json`, `status --json` across local-only/valid/invalid/missing-read-key states, corrective 401/404/422 guidance across login/project-key/revoke/metrics flows, multi-profile isolation with distinct base URLs and credentials, active/non-active instance removal edge cases, malformed filter rejection, and exact repeated-`group_by` query serialization. Config-focused coverage now locks down URL normalization on create/load plus temp-file cleanup for both injected atomic-write failure and repeated successful saves. The repository also now includes `scripts/cli-smoke.sh`, which drives a real local Compose stack through `instances add -> auth login -> projects create -> projects use -> keys create --kind read -> metrics sessions` using a fresh `XDG_CONFIG_HOME`; the latest live run returned `{"granularity":"day","group_by":[],"metric":"count","series":[{"dimensions":{},"points":[{"bucket":"2026-01-01","value":1}]}]}` after ingesting one sample event. Fresh verification in this follow-up: `cargo test -p fantasma-cli --lib`, `cargo test -p fantasma-cli --test cli_config`, `cargo test -p fantasma-cli --test http_flows`, `cargo test -p fantasma-cli`, `cargo fmt -p fantasma-cli -- --check`, `cargo run -p fantasma-cli -- --help`, `XDG_CONFIG_HOME="$(mktemp -d)" cargo run -p fantasma-cli -- status`, `bash -n scripts/cli-smoke.sh`, `./scripts/cli-smoke.sh`, and `git diff --check -- Cargo.toml README.md docs/STATUS.md docs/architecture.md docs/deployment.md docs/superpowers/plans/2026-03-13-cli-test-hardening.md crates/fantasma-cli scripts/cli-smoke.sh`.
- Hardened the operator CLI after review on March 13, 2026. Instance profiles now preserve API path prefixes instead of stripping subpath deployments back to host-root `/v1/*`, `fantasma status` now reports local-only state instead of exiting when no active instance is configured, config loads now reject unsupported future schema versions, and Unix config persistence now writes through a temp-file-and-rename path so a failed save does not clobber the last good operator token or local `read` keys. Deployment docs now call out that CLI instance URLs may include a reverse-proxy path prefix. Fresh verification in this follow-up: `cargo test -p fantasma-cli`, `cargo fmt -p fantasma-cli -- --check`, `cargo run -p fantasma-cli -- --help`, `XDG_CONFIG_HOME="$(mktemp -d)" cargo run -p fantasma-cli -- status`, and `git diff --check -- Cargo.toml README.md docs/STATUS.md docs/architecture.md docs/deployment.md crates/fantasma-cli`.
- Completed the first operator CLI slice on March 13, 2026. The workspace now includes a new `fantasma` binary under `crates/fantasma-cli` for named remote instance profiles, operator-token login/logout, UUID-only active-project selection, explicit project/key management, local per-project `read` key storage, `status` diagnostics that distinguish missing vs invalid credentials, and project-scoped metrics queries with human-readable or `--json` output. The CLI refuses cross-host profile rebinding under an existing instance name, redacts secrets from `instances list --json`, writes config with owner-only permissions on Unix-like systems, and fails closed on non-Unix config persistence rather than silently storing secrets without strict access control. Public docs now treat the CLI as the primary manual operator workflow and leave `scripts/provision-project.sh` in place for automation. Fresh verification in this slice: `cargo fmt -p fantasma-cli -- --check`, `cargo test -p fantasma-cli`, `cargo run -p fantasma-cli -- --help`, and `git diff --check -- Cargo.toml README.md docs/STATUS.md docs/architecture.md docs/deployment.md crates/fantasma-cli`.
- Landed the dim3 -> dim4 event-metrics expansion on March 13, 2026. Event payloads now accept 4 explicit string properties, event-metrics queries now accept 4 total referenced dimensions while keeping `group_by` capped at 2 and final groups capped at 100, worker/store rollups now extend from `total/dim1/dim2/dim3` to `total/dim1/dim2/dim3/dim4` on the canonical `event_metric_buckets_*` lineage only, and forward migration `0012_event_metric_dim4_cleanup.sql` now adds dim4 bucket storage plus bounded dim4 read indexes while dropping obsolete `event_count_daily_*` tables/indexes with safe `IF EXISTS` cleanup semantics. The hard-gated SLO matrix now includes explicit dim4 event queries using `app_version` + `plan` filters with `provider` + `region` grouping, and the docs/schema/OpenAPI/iOS SDK text now all reflect the new `4`-key / `4`-dimension boundary plus the no-fallback bounded-cuboid model. Fresh verification in this slice: `cargo test -p fantasma-core allows_four_explicit_properties -- --nocapture`, `cargo test -p fantasma-core returns_indexed_error_when_more_than_four_properties_are_present -- --nocapture`, `cargo test -p fantasma-api parse_event_metrics_query_accepts_four_referenced_dimensions -- --nocapture`, `cargo test -p fantasma-api parse_event_metrics_query_rejects_five_referenced_dimensions -- --nocapture`, `cargo test -p fantasma-worker max_dimension_event_metrics_fanout_stays_bounded -- --nocapture`, `cargo test -p fantasma-store forward_migration_adds_dim4_bucket_storage_and_cleans_legacy_daily_tables -- --nocapture`, `cargo test -p fantasma-bench slo_grouped_query_matrix_contains_the_required_hard_gate_queries -- --nocapture`, `cargo test -p fantasma-bench --quiet`, and `./scripts/docker-test.sh -p fantasma-store dim4 -- --nocapture`.
- Closed the dim4 benchmark proof loop on March 13, 2026. The bench harness now models expected totals from query semantics instead of assuming every `events_count*` hard gate should sum to the full event volume, which fixed false readiness timeouts for the new filtered dim4 queries during `append-*` and repair seed readiness. The full corrected SLO publication now lives under `artifacts/performance/2026-03-13-dim4-impact-full-r2/` and passes `append-*`, `backfill-*`, `repair-*`, and `reads-*` for `30d`, `90d`, and `180d`, including the new `events_count_day_dim4_grouped` / `events_count_hour_dim4_grouped` hard gates. Fresh verification in this follow-up: `cargo test -p fantasma-bench filtered_dim4_event_hard_gates_do_not_expect_the_full_event_total -- --nocapture`, `cargo test -p fantasma-bench repair_seed_filtered_dim4_event_hard_gates_do_not_include_late_events -- --nocapture`, `cargo test -p fantasma-bench --quiet`, and `cargo run -p fantasma-bench -- slo --output-dir artifacts/performance/2026-03-13-dim4-impact-full-r2`.
- Closed the remaining validation gaps from the bind-limit follow-up on March 13, 2026. `fantasma-store` now has focused regressions proving that chunked upserts still accumulate repeated conflict keys correctly when the shared key lands once per chunk boundary in both the event dim3 and session dim2 paths, and a fresh repair-only SLO publication under `artifacts/performance/2026-03-13-repair-bind-follow-up/` confirms the session-lane follow-up still publishes cleanly end to end. Fresh verification in this slice: `./scripts/docker-test.sh -p fantasma-store conflicts_accumulate_across_chunk_boundaries -- --nocapture`, `cargo run -p fantasma-bench -- slo --output-dir artifacts/performance/2026-03-13-repair-bind-follow-up --scenario repair-30d --scenario repair-90d --scenario repair-180d`, `cargo fmt --check`, `./scripts/docker-test.sh -p fantasma-store --lib -- --quiet`, and `./scripts/docker-test.sh -p fantasma-worker --lib -- --quiet`. The fresh published repair results on this machine are now: `repair-30d` seed ingest `30.673s`, repair ingest `122ms`, `event_metrics_ready_ms 382`, `session_metrics_ready_ms 1_432`, `derived_metrics_ready_ms 1_432`; `repair-90d` seed ingest `104.320s`, repair ingest `413ms`, `event_metrics_ready_ms 962`, `session_metrics_ready_ms 6_059`, `derived_metrics_ready_ms 6_059`; `repair-180d` seed ingest `187.273s`, repair ingest `760ms`, `event_metrics_ready_ms 1_849`, `session_metrics_ready_ms 11_714`, `derived_metrics_ready_ms 11_714`. The evidence trail for this follow-up now includes both store-level bind-limit/cross-chunk coverage and an end-to-end repair publication.
- Added a canonical MIT `LICENSE` file on March 13, 2026, so the repository now carries an explicit OSS license at the root, and `README.md` now points readers to that file without duplicating the full legal text.
- Closed the remaining batched-upsert bind-limit gap on March 13, 2026 after review of the event-lane repair. The original fix had chunked only event metric total/dim1/dim2/dim3 helpers; session metric total/dim1/dim2 rebuild writes still built one unbounded `INSERT ... VALUES ... ON CONFLICT` each, so the same Postgres/sqlx ceiling could still move the failure to the session lane. `fantasma-store` now chunks session metric total/dim1/dim2 upserts with the same bind-budget helper, and store regressions now cover oversized event total/dim1/dim2/dim3 plus session total/dim1/dim2 batches so each helper proves it can cross the Postgres bind threshold safely. Fresh verification in this slice: `./scripts/docker-test.sh -p fantasma-store session_metric_dim2_upserts_handle_batches_above_postgres_bind_limit -- --nocapture` (expected red before the fix with `too many arguments for query: 65540`), `./scripts/docker-test.sh -p fantasma-store upserts_handle_batches_above_postgres_bind_limit -- --nocapture`, `cargo fmt --check`, `./scripts/docker-test.sh -p fantasma-store --lib`, and `./scripts/docker-test.sh -p fantasma-worker --lib`. Published repair/read numbers did not change in this doc slice; this follow-up closes the latent session-lane failure mode and strengthens regression coverage around the bind-budget boundaries.
- Closed the intermittent `repair-30d` SLO failure on March 13, 2026. Root cause: the repair event lane could accumulate enough `event_metric_buckets_dim3` deltas to build one `INSERT ... VALUES ... ON CONFLICT` statement above the Postgres/sqlx bind-argument limit; the worker logs showed the lane retrying `PgConnection::run(): too many arguments for query: 100320`, which left `repair-30d` timing out while waiting for event readiness. The fix keeps the existing set-based write model but chunks event-metric total/dim1/dim2/dim3 upserts by bind-count budget inside `fantasma-store`, plus a new store regression proves a `6000`-row dim3 batch now succeeds instead of failing at `66000` bind arguments. Fresh verification in this slice: `./scripts/docker-test.sh -p fantasma-store event_metric_dim3_upserts_handle_batches_above_postgres_bind_limit -- --nocapture`, `cargo test -p fantasma-bench --quiet`, `docker compose -f infra/docker/compose.bench.yaml config`, `cargo fmt --check`, `./scripts/docker-test.sh -p fantasma-store --lib`, `./scripts/docker-test.sh -p fantasma-worker --lib`, `cargo run -p fantasma-bench -- slo --output-dir artifacts/performance/2026-03-13-repair-event-ready-batch-fix --scenario repair-30d`, and `cargo run -p fantasma-bench -- slo --output-dir artifacts/performance/2026-03-13-repair-event-ready-batch-fix-large --scenario repair-90d --scenario repair-180d`. Fresh published repair results on this machine are now: `repair-30d` seed ingest `31.959s`, repair ingest `115ms`, `event_metrics_ready_ms 486`, `session_metrics_ready_ms 1_553`, `derived_metrics_ready_ms 1_553`; `repair-90d` seed ingest `95.391s`, repair ingest `411ms`, `event_metrics_ready_ms 827`, `session_metrics_ready_ms 5_315`, `derived_metrics_ready_ms 5_315`; `repair-180d` seed ingest `190.000s`, repair ingest `807ms`, `event_metrics_ready_ms 1_626`, `session_metrics_ready_ms 12_686`, `derived_metrics_ready_ms 12_686`. With the event-lane crash removed, the next visible repair bottleneck is now the session rebuild lane rather than repair-path event readiness.
- Ran the next benchmark follow-up on March 13, 2026 after the append rewrite: the read-only sweep published `reads-30d`, `reads-90d`, and `reads-180d` with grouped session hour p95s of `16ms` / `15ms` / `15ms`, `39ms` / `39ms` / `38ms`, and `75ms` / `76ms` / `75ms` respectively for `count`, `duration_total`, and `new_installs`, which keeps the read path comfortably inside budget even at `180d`. The repair sweep did not publish a clean large-window series because `repair-30d` failed on the third repetition with `scenario execution failed: timed out waiting for repair readiness (event metrics)` after two healthy repetitions (`375ms` / `1_443ms` and `373ms` / `1_456ms` for event/session readiness), so the next concrete bottleneck signal after append is now repair-path event readiness rather than grouped session hour reads.
- Restored the Rust CI workflow on March 13, 2026 after stable Clippy 1.92 surfaced new warnings under `-D warnings`. `fantasma-bench` now avoids the new `manual_is_multiple_of` and `redundant_closure` findings directly, `fantasma-worker` and `fantasma-api` absorb the new mechanical lint expectations with type aliases plus small control-flow/value cleanups, and `fantasma-store` explicitly allows `clippy::too_many_arguments` because its metrics query helpers intentionally mirror the public query selector surface instead of hiding it behind a larger refactor during this CI repair. Fresh verification in this slice: `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `./scripts/docker-test.sh --quiet`.
- Completed the session append-delta performance rewrite on March 13, 2026. Append child transactions now leave `sessions`, `install_first_seen`, `install_session_state`, `session_daily_installs`, `session_daily`, and `session_metric_buckets_total` / `dim1` / `dim2` final at commit time; append work is planned per `(project_id, install_id)` in memory and then persisted through net batched tail/session/day/bucket writes; `session_daily` stayed on the hot append path but only as one net per-day delta write set; repair/backfill stayed fully rebuild-based. Fresh verification in this slice: `env DATABASE_URL=postgres://fantasma:fantasma@127.0.0.1:15432/postgres cargo test -p fantasma-worker --lib`, `./scripts/docker-test.sh -p fantasma-store --lib`, `./scripts/docker-test.sh -p fantasma-worker --lib`, `cargo test -p fantasma-bench --quiet`, `cargo fmt --check`, `docker compose -f infra/docker/compose.bench.yaml config`, `cargo run -p fantasma-bench -- slo --output-dir artifacts/performance/2026-03-13-session-append-delta --scenario append-30d`, and `cargo run -p fantasma-bench -- slo --output-dir artifacts/performance/2026-03-13-session-append-delta-large --scenario append-90d --scenario append-180d`. Published append-only results on this machine now are: `append-30d` ingest `33.223s`, `event_metrics_ready_ms 58`, `session_metrics_ready_ms 13_378`, `derived_metrics_ready_ms 13_378`; `append-90d` ingest `94.219s`, `event_metrics_ready_ms 144`, `session_metrics_ready_ms 41_116`, `derived_metrics_ready_ms 41_116`; `append-180d` ingest `197.072s`, `event_metrics_ready_ms 277`, `session_metrics_ready_ms 74_275`, `derived_metrics_ready_ms 74_275`. The `<= 60s` `append-30d` session freshness target is now met and the event lane did not regress.
- Completed the derived-metrics freshness/read-SLA slice on March 13, 2026: `fantasma-worker` now runs concurrent `sessions` and `event_metrics` lanes in one binary, `install_session_state` persists durable per-install replay progress through `last_processed_event_id`, successful child session work enqueues durable pending rebuild buckets so coordinator failures can replay safely, the runtime and default Compose stacks now use lane-specific worker config, and `fantasma-bench` gained a fixed `slo` suite for 30d/90d/180d freshness and grouped-read SLA measurement with visibility-only larger-window read readiness, stale-output cleanup, and failure summaries. Fresh verification in this slice: `cargo fmt --check`, `cargo test -p fantasma-bench --quiet`, `docker compose -f infra/docker/compose.yaml config`, `docker compose -f infra/docker/compose.bench.yaml config`, and `docker compose -p fantasma-tests-review -f infra/docker/compose.test.yaml run --rm workspace-tests cargo test -p fantasma-worker worker::tests::replayed_batch_rebuilds_metrics_after_coordinator_failure -- --exact`.
- Shifted Fantasma's numeric benchmarking to a local/manual workflow on March 13, 2026: deleted the heavyweight GitHub `Performance` workflow, kept the fast benchmark-related checks in normal CI, taught `fantasma-bench` a repeatable `series` subcommand plus a stronger `heavy` profile, rewrote the performance docs around local publication, and checked in a five-run median-backed result set under `artifacts/performance/2026-03-13-m3-pro-heavy/`. Published medians from this Apple M3 Pro / 36 GiB / Darwin 25.1.0 / `aarch64` run: `hot-path` ingested `9,000` events in `305ms` (`29.49k` events/s) with `4.252s` derive readiness; `repair-path` seeded `3,000` events in `105ms` (`28.52k` events/s), repaired `1,200` late events in `44ms` (`26.81k` events/s), with `1.690s` seed readiness and `1.146s` repair readiness; `scale-path` ingested `180,000` events in `5.350s` (`33.64k` events/s) with `96.352s` derive readiness. Fresh verification in this slice: `cargo test -p fantasma-bench --quiet`, `cargo fmt --check`, `docker compose -f infra/docker/compose.bench.yaml config`, and `cargo run -p fantasma-bench -- series --profile heavy --repetitions 5 --output-dir artifacts/performance/2026-03-13-m3-pro-heavy`.
- Followed up on the family-based metrics rewrite after review on March 13, 2026: sessionization now keeps `app_version` fixed to the session's original grouped assignment instead of letting later events win, late-event repair preserves existing grouped session dimensions while still recomputing session timing from raw events, the repair window now includes late events that arrive before the earliest overlapping session start, deleted-route coverage now explicitly checks the removed legacy metrics paths, and the Compose smoke flow now exercises an hourly metrics query in addition to the daily contract. Fresh verification in this follow-up: `cargo test -p fantasma-worker sessionization::tests::`, `cargo test -p fantasma-api --lib`, `./scripts/docker-test.sh -p fantasma-worker --lib`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline`, `bash -n scripts/compose-smoke.sh`, and `./scripts/compose-smoke.sh`.
- Completed the family-based bucketed metrics rewrite on March 13, 2026: the public metrics surface now exposes only `GET /v1/metrics/events` and `GET /v1/metrics/sessions`, every read requires `metric`, `granularity`, `start`, and `end`, both families return the same `metric` / `granularity` / `group_by` / `series` shape, event metrics stay bounded around `count`, session metrics now expose only `count`, `duration_total`, and fixed-assignment `new_installs`, grouped session reads are limited to `platform` and `app_version`, legacy public params such as `project_id`, `start_date`, and `end_date` fail fast with `422`, and worker-owned hourly/daily rollups plus durable install first-seen state now drive the API, docs, smoke flow, and bench harness consistently. Fresh verification in this slice: `cargo test -p fantasma-api --lib`, `cargo test -p fantasma-bench --quiet`, `./scripts/docker-test.sh -p fantasma-api --test auth`, `./scripts/docker-test.sh -p fantasma-worker --lib`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline`, `./scripts/docker-test.sh -p fantasma-store --lib`, and `./scripts/compose-smoke.sh`.
- Hardened the operator-managed auth slice after review: management key routes now return real `404` envelopes for missing resources, management `POST` routes authorize before request-body validation so unauthenticated callers consistently receive Fantasma `401` envelopes, both management create payloads now reject unknown JSON fields to match the published `additionalProperties: false` contract, every session-metric route now rejects unknown public query parameters such as legacy `project_id`, and migration `0008` no longer carries legacy `api_keys` backfill logic because this stack is treated as brand new. Fresh verification in this slice: `cargo test -p fantasma-api --lib`, `./scripts/docker-test.sh -p fantasma-api --test auth`, and `cargo test -p fantasma-store forward_migration_extends_api_keys_for_scoped_project_keys --quiet`.
- Replaced startup-seeded auth with operator-managed provisioning. `fantasma-api` now exposes `/v1/projects*` management routes behind operator bearer auth, project keys are split into `ingest` and `read` kinds with revocation support, public metrics auth moved from bearer-plus-`project_id` to project-scoped read keys, the runtime services now run migrations only, and local smoke/bench/demo flows provision credentials through the API instead of hardcoded bootstrap env vars. Fresh verification in this slice: `cargo test -p fantasma-api --lib`, `cargo test -p fantasma-bench --quiet`, `./scripts/docker-test.sh -p fantasma-api --test auth`, `./scripts/docker-test.sh -p fantasma-worker --test pipeline`, `./scripts/docker-test.sh -p fantasma-store api_key_lifecycle_round_trips_kinds_and_revocation`, `./scripts/compose-smoke.sh`, `bash -n scripts/compose-smoke.sh scripts/provision-project.sh`, `docker compose -f infra/docker/compose.yaml config`, and `docker compose -f infra/docker/compose.bench.yaml config`.
- Stabilized GitHub Actions after the March 12, 2026 CI investigation: `fantasma-bench` renamed its internal benchmark scenarios to avoid the deterministic Clippy failure that was redlining the `CI` workflow, gained a retained-runner regression that evaluates the committed benchmark budgets against representative `main` workflow artifacts, and recalibrated the hot-path and repair-path CI thresholds to match the retained runner floor instead of failing on normal GitHub variance. Fresh verification in this slice: `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test -p fantasma-bench --quiet`, `cargo test -p fantasma-bench tests::retained_ci_runner_samples_fit_committed_budgets -- --exact`, and `./scripts/docker-test.sh --quiet`.
- Refocused the deployment docs around actual stack bring-up and operator verification: `docs/deployment.md` now documents the Compose topology, runtime configuration, startup behavior, preconditions, bring-up, smoke flow, and manual health checks, while architecture/query-policy text and contributor test workflow details were removed from the deployment runbook and the iOS demo walkthrough moved to `apps/demo-ios/README.md`.
- Hardened the Swift SDK for Swift 6 structured concurrency: `Fantasma` now exposes async/throwing static APIs with typed `URL` configuration, the shared runtime lazily creates the core without blocking or `try!`, queue and identity storage now sit behind actor isolation, explicit failure modes are covered by Swift Testing regressions, explicit `flush()` calls now correctly join timer/background/threshold-owned uploads, `clear()` still rotates identity if live-core creation fails, docs/demo usage now show `try await`, and CI runs the iOS package with complete concurrency checking using a portable `python3` launcher in the ingest round-trip test helper.
- Hardened the performance-proof slice after review: `fantasma-bench` now scopes Docker commands to the dedicated `fantasma-bench` Compose project, benchmark services moved to isolated host ports so local runs cannot tear down the default dev stack, startup cleanup now fails closed instead of benchmarking against dirty state, the worker catch-up regression now proves at least one grouped event-metrics request ran during concurrent catch-up, and the performance docs now point at the real worker test command.
- Fixed a consistency bug in the event-metrics query path: grouped event-metrics reads now run inside one repeatable-read snapshot instead of stitching multiple cube selects across live worker commits, and a new DB-backed pipeline test locks that regression down by hammering `GET /v1/metrics/events/daily` while the event-metrics worker catches up one batch at a time.
- Recalibrated the enforced CI benchmark budgets from retained GitHub `ubuntu-latest` artifacts on March 12, 2026. Retained medians came in at `12.35k` events/s and `523ms` ready for `hot-path`, `11.47k` / `13.00k` events/s with `347.5ms` / `342ms` readiness for `repair-path`, and `11.51k` events/s with `13.56s` readiness for `scale-path`; readiness/query ceilings now still use `20%` median slack, but they never tighten below the slowest retained runner sample.
- Added performance proofing for Fantasma's bounded backend paths: PR CI now locks the worker fanout boundary and benchmark Compose config, the repository has a first-class `fantasma-bench` harness plus `infra/docker/compose.bench.yaml`, and a dedicated `performance.yml` workflow now runs numeric hot-path and repair-path stack benchmarks on `main` and manual dispatch.
- Clarified the event-property cap across public docs: `README.md` now states only the high-level principle that Fantasma keeps event context narrow for predictable performance, while the event schema, OpenAPI, and architecture docs carry the exact 3-key limit and its worker-aggregate rationale.
- Refocused `README.md` into a product-facing entry point instead of a contract dump, and tightened `AGENTS.md` so future contributors keep detailed implementation semantics in deployment, architecture, SDK, and schema docs instead of pushing them back into the README.
- Followed up on the docs-centralization pass by removing repeated `install_id` policy prose from deployment, architecture, and SDK docs. `README.md` stays the canonical explanation; derived docs now focus on local contract or behavior instead of re-explaining the same privacy boundary.
- Centralized Fantasma's public direction after the install-scoped event-metrics work: `README.md` now acts as the canonical product/privacy statement, `AGENTS.md` tells contributors not to spread that stance across multiple docs, and deployment, architecture, SDK, OpenAPI, and event-schema text now describe the same install-scoped/eventually-consistent contract instead of restating it with drift.
- Refined the event-metrics dim2/dim3 read path after review: forward migration `0007_event_metrics_read_indexes.sql` replaces the older slot-ordered indexes with key/value-aware index coverage so canonical later-slot filters still stay on a bounded indexed path, and store migration tests now lock that index set in place.
- Tightened the worker-derived event-metrics slice after review: group-limit failures now run through bounded aggregate prechecks before the full daily synthesis path, migration history is forward-only again via `0006_remove_legacy_identity_columns.sql`, and the published OpenAPI now documents structural ingest `invalid_request` responses, event-metrics `500` envelopes, and the explicit-property filter namespace.
- Replaced the raw event-count route with worker-derived event metrics: `GET /v1/metrics/events/aggregate` and `GET /v1/metrics/events/daily` now read from bounded daily Postgres rollups, support explicit filtering and grouping, synthesize null buckets, hard-fail on group overflows, and use inclusive UTC `start_date` / `end_date` semantics.
- Added the event-metrics rollup storage slice in Postgres with forward migration `0005_event_metrics_rollups.sql`, `os_version` on `events_raw`, worker-owned `event_count_daily_total` plus 1/2/3-dimension cuboids, database-enforced canonical slot ordering, and end-to-end worker processing in both tests and the long-running worker service.
- Narrowed the public event contract to bounded explicit string-only properties, added built-in `os_version`, preserved indexed ingest validation through raw DTO normalization, updated JSON Schema/OpenAPI/deployment docs, and updated the iOS SDK docs/tests/examples so supported clients auto-populate `platform`, `app_version`, and `os_version`.
- Removed public `user_id` and client-sent `session_id` from Fantasma's MVP contract, rewrote the undeployed SQL migration history to the install-scoped shape, kept backend session IDs internal-only, removed `identify(_:)` from the iOS SDK, and updated docs/examples/scripts to match.
- Updated the Compose smoke flow to reset its disposable Postgres volume before and after each run so schema rewrites do not leave stale migration history behind.
- Implemented Fantasma's first derived metric: `fantasma-worker` now derives sessions from `events_raw`, stores them in `sessions`, tracks progress in `worker_offsets`, and exposes `GET /v1/metrics/sessions/count`, `GET /v1/metrics/sessions/duration`, and `GET /v1/metrics/active-installs`.
- Fixed the Swift SDK CI hang in the destination-drift upload test by removing a competing explicit flush from the `uploadBatchSize = 1` scenario, and hardened the suspended transport helper so concurrent send-start waiters and releases are tracked per send instead of by a single continuation slot.
- Replaced startup bootstrap DDL with `sqlx` migrations in `crates/fantasma-store/migrations/` and split schema preparation into always-run migrations plus optional local project seeding.
- Added bounded exact-day historical repair to the tail-state worker: out-of-order install batches now recompute only the overlapping derived sessions, keep the read/delete/write sequence inside one transaction, and rebuild only the affected UTC session-start days.
- Added `session_daily_installs` so daily active installs are maintained from explicit membership state instead of `COUNT(DISTINCT ...)` rebuilds.
- Kept daily metrics incremental and Postgres-only: `session_daily` still stores UTC `DATE` buckets for `sessions_count`, `active_installs`, and `total_duration_seconds`, with the hot append path updated incrementally and the repair path rebuilding only exact touched days.
- Exposed explicit daily-series endpoints for `GET /v1/metrics/sessions/count/daily` and `GET /v1/metrics/sessions/duration/total/daily` with inclusive `start_date` / `end_date` query semantics and zero-filled UTC series responses.
- Extracted reusable library entrypoints for ingest, API, and worker batch processing so in-process end-to-end tests can drive `POST /v1/events`, run one worker batch, and query daily metrics without spawning services.
- Added a preflighted Compose smoke script that checks disk and Docker availability, ingests sample events, polls the daily metrics endpoint, and dumps logs on timeout.
- Recorded the repository preference that DB-backed Rust tests should run fully in Docker, with workspace Postgres tests separated from stack-level smoke verification.
- Added a dedicated Docker test workflow with `infra/docker/compose.test.yaml`, `infra/docker/Dockerfile.test`, and `./scripts/docker-test.sh` so DB-backed Rust tests can run fully inside Docker against containerized Postgres using `sqlx::test`, without requiring host-user writes to the Cargo cache volumes.
- Added DB-backed store and worker tests for tail state persistence, bounded out-of-order repair, exact-day daily rebuilds, checkpoint round-trips, incremental daily metric updates, and daily install-membership tracking.
- Updated OpenAPI, architecture, deployment docs, and CI so the repository documents and tests the `events_raw -> worker -> sessions -> metrics API` slice.
- Updated `AGENTS.md` to align Fantasma's agent workflow with the installed `superpowers` skills, including expectations for planning, debugging, verification, subagent execution, and review.
- Implemented the first iOS SDK prototype as a Swift Package with explicit `configure`, `track`, `identify`, `flush`, and `clear` APIs, a durable SQLite-backed event queue, and asynchronous uploads to `POST /v1/events`.
- Added Swift tests covering event serialization, queue persistence, queue replay after failures, batch deletion rules, and identity rotation semantics.
- Added a demo iOS app that exercises the SDK against the local development ingest service with explicit `app_open`, `screen_view`, and `button_pressed` events.
- Implemented vertical slice 1: `POST /v1/events` now validates batches, resolves project-scoped ingest keys from Postgres, and inserts raw events into `events_raw`.
- Added startup Postgres bootstrap for `projects`, `api_keys`, and `events_raw`, including raw-event indexes for project/time, install, event name, platform, and received time.
- Added `GET /v1/metrics/events/count` with bearer admin auth and direct counting from `events_raw`.
- Relaxed the initial event contract to support string `install_id`, optional `app_version`, smaller batch limits, and payload-size guardrails.
- Updated local deployment defaults, OpenAPI, JSON schemas, and architecture/deployment docs for the first end-to-end ingest slice.
- Captured repository operating rules in `AGENTS.md`.
- Defined documentation-first workflow and project memory rules.
- Documented the initial Fantasma architecture and deployment targets.
- Bootstrapped the Cargo workspace and monorepo skeleton.
- Added shared event and metric domain types in `fantasma-core`.
- Added initial auth primitives and runnable ingest, API, and worker service binaries.
- Added the first public OpenAPI contract and event schema files.
- Added Docker-based local deployment scaffolding and the initial extra client placeholder that was later removed from repository scope.
- Added GitHub Actions CI for formatting, linting, tests, schema JSON validation, and Compose config checks.

## Next

- Run the end-to-end iOS Simulator smoke test against the local stack and confirm the demo app drives the new worker-derived event metrics alongside the existing session metrics.
- Run a Flutter device or emulator smoke test against the local stack and confirm the new package delivers durable queued events and built-in locale metadata through the real mobile runtime, not only unit tests.
- Decide whether the next aggregate slice should extend event metrics to more product-facing use cases such as release adoption or screen-view reporting, while preserving the same bounded incremental model.

## Open Decisions

- Whether future late-event handling should stay bounded to per-install exact-day repair or eventually add a separate explicitly scoped backfill workflow.
- Whether the Flutter SDK should keep `storageNamespace` as a required explicit boundary or later grow a higher-level single-client convenience API for common one-app installs.
