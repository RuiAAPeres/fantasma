# Fantasma iOS SDK

The first SDK implementation target is iOS.

Required public API:

- `configure(serverURL, writeKey)`
- `track(eventName, properties?)`
- `identify(userId)`
- `flush()`
- `clear()`
